# 14 — финальная QA-таблица P12

**Итог: 13 PASS из тринадцати.** *(Обновлено 2026-09-22: единственный `FAIL` закрыт решением `D-050`.)*

~~Итог первого прогона: 12 PASS, 1 FAIL.~~ Единственный `FAIL` — `terminology`:
глоссарий вырос с замороженных 47 статей до 49, и одна из двух новых
(«Основное действие») не имеет записи в карте терминов. Заведено `TISS-012`,
печать не блокирует.

- Дата прогона: **2026-09-22**, повторный после правок `D-047`
- Предмет проверки: `out/RULEBOOK.md` — 2421 строка, 114 120 символов, части 1–13
- Инструменты: `tools/qa_final.py` (тринадцать проверок), `tools/qa_lib.py`
  (общая часть), `tools/build_rulebook.py --verify` (сборка и доказательство
  переноса). Полный вывод — `out/14-final-qa-run.txt` и `out/14-build-verify.txt`
- `registry.py all` — `EXIT=0`, восемь отчётов пересобраны; `map.py check` —
  `EXIT=0`

Все числа ниже посчитаны скриптами. Там, где порог автоматической меры оказался
ниже фактического покрытия, место в книге подтверждено чтением раздела,
и это сказано прямо, с цитатой.

---

## Таблица

| № | Проверка | Вердикт | Число |
|---|---|---|---|
| 1 | mechanical parity | **PASS** | 97 из 97 записей `VERIFIED` отражены в тексте; 9 выведены решениями |
| 2 | numerical parity | **PASS** | `PASS` 73, `FAIL` 5, `UNKNOWN` 61; все 5 `FAIL` закрыты решениями |
| 3 | timing parity | **PASS** | 41 из 41 записи эталона с полем `timing`; 1 выведена решением |
| 4 | interaction parity | **PASS** | 23 из 23 стыковых находок P10/P11: 21 скриптом, 2 чтением раздела |
| 5 | setup | **PASS** | 3 состава из 3 сходятся с реестром карты; 6 опорных чисел подготовки на месте |
| 6 | victory | **PASS** | 5 из 5 записей `BL-VICTORY-*` отражены в частях 9 и 10 |
| 7 | terminology | **PASS** | 49 статей против 47 замороженных, 2 сверх состава; 27 отменённых написаний — 0 вхождений; латиницы 0 |
| 8 | icons | **PASS** | 118 вхождений плейсхолдеров, 34 разных — все в `icons.yaml`; emoji 0 |
| 9 | cross-references | **PASS** | 189 из 189 ссылок ведут в существующий раздел |
| 10 | legacy terminology | **PASS** | 13 шаблонов отменённой терминологии — 0 вхождений |
| 11 | edge cases | **PASS** | 30 из 30 пограничных случаев двух партий разрешаются по чистовику |
| 12 | markdown | **PASS** | 5 таблиц валидны, прозаических строк длиннее 90 — 0, HTML 0, табуляций 0 |
| 13 | playthrough | **PASS** | 49 из 49 находок `APPLIED`: 42 скриптом, 7 чтением раздела |

Сверх тринадцати — требования к самому файлу чистовика: следов процесса **0**,
запрещённых слов `D-003` **0**, плейсхолдеры иконок сохранены (**118**), части
идут подряд с 1 по 13.

---

## 1. Mechanical parity — PASS

**97 из 97.** Метод: слова правила эталона прогоняются через `terms.yaml`
(`original_en` и `original_ru` → `redesign`), режутся до основы и ищутся
в тексте книги; порог покрытия **0.70**. Медиана покрытия по корпусу — **0.89**.

Девять записей `VERIFIED` выведены из книги решениями и в измерение не входят:

| Запись | Почему выведена |
|---|---|
| `BL-MODULE-001`…`BL-MODULE-007` | игра на двоих не описывается (`D-021`) |
| `BL-UNIT-010` | запись отрицательная: фиксирует, что типа отряда «здание» нет |
| `BL-UNIT-012` | способность конкретного юнита, живёт на планшете (`D-021`) |

Чего проверка **не** доказывает: она измеряет присутствие правила, а не его
смысловую точность. Смысловая сверка с эталоном — это P9, четыре независимых
прохода; здесь проверяется, что при сборке чистовика ничего не выпало.

## 2. Numerical parity — PASS

**`PASS` 73, `FAIL` 5, `UNKNOWN` 61** из 139 записей `numbers.yaml`.

Все пять `FAIL` разобраны и закрыты решениями автора — самостоятельных находок
среди них нет:

| Запись | Число | Находка | Решение |
|---|---|---|---|
| `NUM-001` | стартовый запас ресурса | `DEV-002` | `D-035` |
| `NUM-002` | разметка трека запаса | `DEV-005` | `D-020` |
| `NUM-003` | число этапов фазы влияния | `DEV-003` | `D-020` |
| `NUM-048` | «заворачивание» трека при превышении разметки | `DEV-005` | `D-020` |
| `NUM-076` | число шагов битвы | `DEV-010` | `D-026` |

Шестьдесят один `UNKNOWN` — числа, живущие на компонентах: они вне объёма работы
над правилами (`D-004`) и в книгу не выносятся.

## 3. Timing parity — PASS

**41 из 41.** Проверяются моменты срабатывания: поле `timing` каждой записи
эталона против текста книги, порог **0.65**. Выведена одна запись —
`BL-UNIT-008`: её `timing` описывает момент срабатывания способности полковника,
а она напечатана на планшете (`D-021`).

Опорные тайминги, вошедшие в измерение: четыре фазы раунда и их порядок,
пропуск первых трёх фаз в первом раунде, два шага хода, очередь оказания
давления, три фазы битвы и два такта финальной фазы, момент окончания партии.

## 4. Interaction parity — PASS

**23 из 23.** Берутся находки P10 и P11 со статусом `APPLIED`, у которых
в категории стоит `INTERACTION`, `SEQUENCE`, `TIMING` или `EXCEPTION` — то есть
ровно стыки правил, по которым били обе фазы. Для каждой проверяется, что
названные в записи разделы в книге есть и несут её содержание.

Двадцать одна подтверждена скриптом. Две подтверждены чтением раздела — запись
решения у них наполовину состоит из разбора источника, и словарная мера
занижает покрытие:

- `PLAY-003` → **8.5**: «Один отряд — один результат», «Отряд, которому назначена
  смерть или уничтожение, дальше целью не считается», «Если способность позволяет
  отряду пережить назначенную смерть»;
- `PLAY-004` → **8.3** «Отряд, выведенный из битвы, но оставшийся в регионе,
  с поля не убирается» и **8.9** «исключены из битвы (8.3)».

## 5. Setup — PASS

Число регионов в таблице 2.1 сверено с подреестром карты
(`registry/map/configs.yaml`), а не с памятью:

| Игроков | В книге | В реестре карты |
|---|---|---|
| 3 | 13 | 13 (`MC-3P`) |
| 4 | 17 | 17 (`MC-4P-A`, `MC-4P-B`) |
| 5 | 21 | 21 (`MC-5P`) |

Плюс шесть опорных чисел подготовки найдены в тексте поимённо: шесть рекрутов,
одна контролируемая фабрика, маркер влияния на 0, маркер Судного дня на 5,
стартовые 8 нефти, шесть океанских регионов.

Для 6–8 игроков чисел нет ни в оригинале, ни у нас — в таблице стоят прочерки
(`D-040`).

## 6. Victory — PASS

**5 из 5** записей `BL-VICTORY-*` отражены в частях 9 и 10 (плюс 3.1, 6.4, 6.8),
порог **0.60**: два условия окончания, момент окончания, раскрытие скрытого
влияния, определение победителя, последний вопрос.

## 7. Terminology — PASS *(после `D-050`)*

Что сошлось:

- **27 отменённых написаний** из `terms.yaml` — «локация», «адепт», «боевая
  единица», «книга технологий», «ядерный катаклизм», «стартовая зона»
  и прочие — **0 вхождений**;
- **латиницы 0** (плейсхолдеры иконок и римская цифра века не в счёт);
- **вне книги — ни одного из 75 канонических терминов**: 70 стоят дословным
  написанием записи, ещё пять — в склонении или раздельно (`TERM-018` «Наёмные
  боевые машины» в 11.4.1, `TERM-047` «задачей, требующей выполнения действия»
  в 7.5, `TERM-071` «памятка» в 1.1 и 2.2, `TERM-026` и `TERM-070` — сводные
  ярлыки реестра, а не написания книги);
- все 49 статей опираются на записи `CANONICAL`.

Что не сошлось — **два термина мимо карты**:

| Статья | Что не так |
|---|---|
| «Карта лояльности» | запись в карте терминов есть (`TERM-020`, `CANONICAL`), в таблице `STRUCTURE.md` §13 — нет |
| «Основное действие» | нет ни в таблице `STRUCTURE.md` §13, ни в `terms.yaml` вовсе |

Замороженный состав — 47 статей, в книге их 49. Обе статьи написаны верно
и нужны: «Карта лояльности» появилась с разбором наёмников (`D-038`, `D-045`),
«Основное действие» — следствие `D-037`, который завёл пятую категорию действий
уже после фриза терминологии в P7. Нарушены два правила устава: состав глоссария
меняется только решением, а термин книги обязан иметь запись в карте.

**Закрыто решением `D-050` (2026-09-22).** Состав расширен до сорока девяти
статей, обе остаются. Заведена запись `TERM-080` «Основное действие» — наш
термин, прямого соответствия в оригинале нет; «Карта лояльности» получила
строку в таблице `STRUCTURE.md` §13, запись `TERM-020` у неё была. Состав
заморожен заново.

Удалить две статьи ради формального соответствия фризу отвергнуто: фриз заведён
против молчаливого расползания состава, а не против обоснованных добавок.
`TISS-012` — `CLOSED`.

## 8. Icons — PASS

**118 вхождений, 34 разных плейсхолдера**, все есть в `icons.yaml` (44 записи).
Emoji — **0**, включая посимвольный поиск по категории Unicode `So`. Плейсхолдеры
оставлены как есть и заменяются глифами при вёрстке (`D-029`).

## 9. Cross-references — PASS

**189 из 189** ссылок ведут в существующий раздел. Разобраны обе формы: ссылка
в скобках в теле правил — `(7.3.1)` — и сноска в конце статьи глоссария —
`— 8.1, 8.2`. Дерево заголовков даёт **86** разделов с номером.

## 10. Legacy terminology — PASS

Тринадцать шаблонов терминологии, отменённой решениями, — **0 вхождений**:

- `D-019`: «отступление» в роли назначаемого результата («назначить
  отступление», «результат отступление», «получает отступление», старая таблица
  граней кубика, иконка `{SUPPRESS}` при слове «отступление»), «ликвидация»,
  «списание отряда». Отступление как **применение** подавления остаётся: это
  название §8.7 и штатный термин;
- `D-024`: «из запаса» о фигурках вместо «из резерва», «запас фракции».
  «Запас нефти» и «трек запаса нефти» — законны, это ресурс;
- `D-022`: «боевая единица»; `D-029`: «неограниченная битва», «ядерный
  катаклизм»; `D-034`: «незанятая военная фабрика».

## 11. Edge cases — PASS

**30 из 30** пограничных случаев обеих симулированных партий — тринадцать
из P10 и семнадцать из P10B. Для каждого проверено три вещи: названные случаем
разделы в книге есть; каждая порождённая им находка доведена до терминального
статуса; постановка случая покрыта текстом этих разделов.

Случаи, которые в партии **не разрешались** по тексту, закрыты правками: битва,
оборвавшаяся до бросков (`PLAY-004` → 8.9), смерть и подавление одному отряду
(`PLAY-003` → 8.5), 30 влияния без шести технологий (`PLAY-011` → 3.8), давление
после Ядерной катастрофы (`PLAY-016` → 6.7), запас нефти как открытая информация
(`PLAY-007` → 4.2).

## 12. Markdown — PASS

Собственный линтер, шесть требований:

| Требование | Результат |
|---|---|
| иерархия заголовков без скачков уровня | 87 заголовков, скачков 0 |
| валидность таблиц (колонки, разделитель, пустая строка перед) | 5 таблиц, дефектов 0 |
| пустая строка перед каждым списком | нарушений 0 |
| длина строки до 90 символов | прозаических строк длиннее 90 — **0** |
| HTML-вставки и табуляции | 0 и 0 |
| парность ограждений кода, перевод строки в конце файла | в порядке |

Шесть строк длиннее 90 символов — **строки таблиц**, и это неустранимо: перенос
строки внутри таблицы Markdown её ломает. При вёрстке PDF ширина таблицы задаётся
стилем, а не исходником.

## 13. Playthrough — PASS

**49 из 49** находок `PLAY-` и `RED-` со статусом `APPLIED` внесены в текст.
Сорок две подтверждены скриптом, семь — чтением названного раздела:

| Находка | Где нашлось |
|---|---|
| `PLAY-003` | 8.5 — «Один отряд — один результат»; «Отряд, которому назначена смерть или уничтожение, дальше целью не считается»; «Если способность позволяет отряду пережить назначенную смерть» |
| `PLAY-004` | 8.3 — «Отряд, выведенный из битвы, но оставшийся в регионе, с поля не убирается»; 8.9 — «исключены из битвы (8.3)» |
| `PLAY-033` | 3.6 — «Фабрика, которую нельзя взять под контроль… всегда считается фабрикой под контролем своего владельца»; 4.1 — «незанятой она не считается»; 6.2 — «идёт в счёт наравне с контролируемыми» |
| `RED-012` | 7.5 — «Требование, направленное на всех противников, выполняется для каждого, для кого оно выполнимо» |
| `RED-014` | 6.5 — «Каждый такой эффект срабатывает один раз за фазу — в вашу очередь» |
| `RED-018` | 8.7 — «Учтите размен: окружение стоит противнику одного отряда, но пехотинца с фабрики не сдвигает» |
| `PLAY-029` | 2.1 — «На четверых поле бывает двух вариантов, и это разные партии… Играйте на любом». Умолчание переписано без половин поля решением `D-047` |

---

## Независимая проверка сборки

Отдельный проход субагента, которому не сообщалось, кто собирал чистовик.
Задача — один вопрос: не потерялось ли и не изменилось ли хоть одно правило
при сборке. Вердикт: **правила при сборке сохранены.**

Найдено шесть расхождений, ни одно не меняет смысла: две снятые служебные врезки
«Статус черновика», заголовок части 13 вместо заголовка файла глоссария,
переставленные слова в подводке глоссария, добавленный разделитель `---` между
частями 12 и 13, две переверстанные длинные строки.

Чем доказано (числа субагента): `diff` — 13 изменённых строк; `SequenceMatcher`
по словам — 2 неравных блока в частях 1–12 и 2 в глоссарии; мультимножества
сущностей — плейсхолдеры 87/87 и 31/31, перекрёстные ссылки 202/202 и 82/82,
цифровые группы 693/693 и 197/197, строки таблиц 31/31, пункты списков 127/127;
частотная сверка 31 модального и квантифицирующего слова («может», «должен»,
«за ход», «за действие», «любой», «каждый», «до», «после») — **0 расхождений**;
обратная реконструкция чистовика из двух черновиков совпала полностью.

Своя сборочная проверка говорит то же самое независимо: `build_rulebook.py
--verify` сверяет чистовик с черновиками слово в слово — **17 211 против 17 211**.

---

## Что блокирует печать

**Из этой фазы — ничего не осталось.** Единственный `FAIL` (`TISS-012`) был
расхождением
реестров с книгой, а не дефект текста.

Открытых находок в реестре шесть: пять компонентных (`DEV-014`, `MISS-014`,
`DEV-021`, `SRC-003`, `MISS-025`) и заведённая здесь `TISS-012`. **Книгу
не блокирует ни одна** — разобрано решением `D-047`, раздел 5.

Печать блокируют две компонентные работы, решённые по существу и не сделанные:

| Что | Находка | Почему блокирует |
|---|---|---|
| Числа «Очередь» не распределены: три фракции носят «10» | `MISS-018` `DECIDED`, `SRC-003` `OPEN` | §2.4 ссылается на эти числа; `D-031` прямо говорит: до распределения печатать нельзя. **Alek разбирает сейчас**, фиксация — в реестре фракций |
| Правила Луны не напечатаны на планшете Moon Systems | `RED-006`, решение `D-046` | §3.3 отсылает к планшету; без него фракция не имеет ни одной контролируемой фабрики за партию |

Остальное — компонентный бэклог `D-004`, печать книги он не держит:

- **боевая сила Роя** (`MISS-014`): карта Контроля сети ещё не свёрстана,
  число **2** проставляется при вёрстке (`D-040` пункт 4);
- **стоимость карт лояльности наёмников** (`DEV-014`): в книге чисел нет
  вовсе — §6.6, §11.4.1 и §11.4.2 говорят «оплатите стоимость, указанную
  на карте». При вёрстке карт проставить **2 влияния и 2 нефти** (`D-035`);
- **«Стелс»** (`MISS-025`): свести карту и архивное приложение к одной
  формулировке; эталон — карта. §8.3 от этого не зависит;
- **символы регионов** (`MF-011`, `CLOSED`): размещение зафиксировано
  в `registry/map/glyphs.yaml` (`region_symbols`, `MS-001`…`MS-003`) и проверяется
  `map.py check`; нанести на полотно — работа по карте;
- **число полей в комплекте**: книга о нём молчит намеренно, пока форма
  компонента не закреплена (`D-047`, `MISS-023`).

---

## Правки D-047 и их проверка

После первого прогона Alek снял из книги описание физического устройства поля,
компонентные оговорки про состав 6–8 и переформулировал §11.2. Решение —
`D-047`, правок семь, внесены в черновики, чистовик пересобран скриптом.

**Независимый проход по правкам нашёл шесть мест, все исправлены** (разбор —
`D-047` раздел 7). Главное: §2.1 остался с «двумя вариантами поля», но признак,
по которому их различают, ушёл вместе с половинами; преамбула части 11
объявляла все режимы выключенными, а полковники теперь включены по умолчанию;
полковника не было в списке фигурок фракции §1.2.

Повторный прогон после исправлений: **12 PASS, 1 FAIL**, тот же `TISS-012`.
Третий прогон, после решения `D-050`: **13 PASS**, открытых `FAIL` нет.
Сборочная сверка — **17 190 слов против 17 190**.

## Приложения

- `out/14-final-qa-run.txt` — полный вывод `qa_final.py` (все тринадцать проверок
  плюс проверка требований к чистовику)
- `out/14-build-verify.txt` — вывод `build_rulebook.py --verify`
- `tools/qa_final.py`, `tools/qa_lib.py`, `tools/build_rulebook.py` — сами скрипты
