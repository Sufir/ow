# DECISIONS — журнал решений

Append-only. Каждое решение принимает **Alek**. На решение ссылаются issue
(`decision_ref`) и правки в тексте правил.

Формат записи:

```
## D-### — краткая формулировка
- Дата:
- Контекст:
- Решение:
- Следствия:
- Связанные issue:
```

---

## D-001 — Предмет аудита: только гуглдок «Правила (черновик)»

- **Дата:** 2026-09-10
- **Контекст:** текст правил существует в двух местах — гуглдок и
  `Drafts/Нефтяные войны (правила).pdf`. Неизвестно, какой новее.
- **Решение:** предметом аудита считается **только гуглдок**
  `docs.google.com/document/d/1Eb49tnE9XtmucsIQImUPP2uk2OEXHUC292uYP6iSz30`.
  PDF в `Drafts/` — архив, эталоном не является и на расхождения не проверяется.
- **Следствия:** правки PDF, не перенесённые в гуглдок, будут потеряны. Риск принят.
- **Связанные issue:** —

## D-002 — Источники оригинала собирает Claude, недостающее докачивает Alek

- **Дата:** 2026-09-10
- **Контекст:** в папке проекта нет оригинального рулбука Cthulhu Wars, эрраты и FAQ.
  Есть только `Cthulhu_Wars_Faction_Selection_Cards.zip`.
- **Решение:** Claude сам тянет всё, что доступно (necronomicon.app, Petersen Games,
  fandom, открытые файлы). Что достать не удалось — выносится точным списком, Alek
  докачивает руками.
- **Следствия:** baseline может стартовать с частью записей `confidence: MEDIUM/LOW`;
  такие записи помечаются и дособираются после получения официальных PDF.
- **Связанные issue:** —

## D-003 — Объём: один цельный рулбук, включая дополнения, эррату и FAQ

- **Дата:** 2026-09-10
- **Контекст:** правила разнесены по основному документу и приложениям
  (особые фабрики, наёмники, уникальные полковники), плюс существуют официальные
  эррата и FAQ Petersen Games.
- **Решение:** делаем **один рулбук**, куда входят базовые правила, правила
  реализованных дополнений, применённая эррата и разрешения официального FAQ.
  Текст пишется так, будто это изначально были единые целостные правила: без слов
  «дополнение», «модуль», «эррата», «FAQ», «в базовой игре» и без отсылок к оригиналу.
- **Следствия:** объём baseline существенно шире базовой коробки. Правила, которые и в
  оригинале являются вариантом игры по выбору игроков, остаются вариантом, но подаются
  без указания на происхождение.
- **Связанные issue:** —

## D-004 — Фракции вне текущей задачи

- **Дата:** 2026-09-10
- **Контекст:** составы фракций, технологии, карты, жетоны и планшеты требуют отдельной
  выверки.
- **Решение:** сейчас работаем **только с правилами**. От фракций нужны только названия
  и их соответствие оригинальным. Выверка и доработка фракций — отдельная задача.
- **Следствия:** замечания по компонентам не чинятся, а складываются в `issues.yaml`
  с `type: OUT-OF-SCOPE`.
- **Связанные issue:** —

## D-005 — Порядок фаз определяет Claude; реестры машиночитаемы

- **Дата:** 2026-09-10
- **Контекст:** Alek не имеет предпочтений по порядку фаз, но хочет структурности и
  возможности автоматизации.
- **Решение:** порядок фаз — по `PLAN.md` (черновая карта терминов до аудита, эталон
  оригинала до извлечения редизайна, аудит и решения батчами). Реестры ведутся в
  **YAML** как источнике истины; SQLite и отчёты — производные артефакты, собираются
  скриптом `tools/registry.py`.
- **Следствия:** правки вносятся в YAML, база и отчёты пересобираются, а не правятся.
- **Связанные issue:** —

## D-006 — Новые файлы раскладываются структурно

- **Дата:** 2026-09-10
- **Контекст:** файлы в корне проекта лежат хаотично; наведение порядка — вне текущей
  задачи.
- **Решение:** существующий беспорядок не трогаем. Всё новое кладём по структуре:
  служебное — в `.agents/`, исходники — в `.agents/sources/`, черновые сборки — в
  `.agents/out/`, итоговый рулбук — в корень проекта.
- **Следствия:** —
- **Связанные issue:** —

## D-007 — Рабочая позиция: редактор + последний QA, а не аудитор

- **Дата:** 2026-09-10
- **Контекст:** постановка задачи давала большой аудиторский аппарат (фазы, матрицы,
  отчёты), из-за чего продуктом легко подменить рулбук отчётом о расхождениях.
- **Решение:** работаем как редактор правил, который одновременно последний QA перед
  публикацией. Продукт — **готовый к печати рулбук**; реестры, матрицы и отчёты —
  инструмент, а не результат. Всё, что не меняет игровое поведение (язык, структура,
  ясность, ссылки, единство терминов, полнота), правится сразу и молча. Issue-процесс
  включается только там, где затрагивается механика.
- **Следствия:** к Alek идёт не полный список находок, а только то, что не закрывается
  источниками и меняет игровое поведение. Планка приёмки — «по этому тексту можно
  сыграть партию», проверяется в P10. Развёрнуто в `RULES-CHARTER.md` §1.
- **Связанные issue:** —

## D-008 — Claude Code как исполнитель механической работы

- **Дата:** 2026-09-10
- **Контекст:** Linux-песочница Cowork недоступна (три одинаковых отказа монтирования
  подряд). Нет запуска Python и извлечения текста из PDF.
- **Решение:** редактор (Cowork) остаётся автором и редактором, Claude Code — помощник
  и исполнитель. ТЗ для Code кладутся в `.agents/tasks/CODE-NN-*.md`, Alek передаёт их
  в Code, Code отчитывается дописыванием в файлы проекта.
- **Следствия:** Code не редактирует правила и не принимает решений по механике —
  только доставка, извлечение, скрипты и проверки. Границы прописываются в каждом ТЗ.
- **Связанные issue:** —

## D-009 — Эталон оригинала: рулбук в новой вёрстке

- **Дата:** 2026-09-10
- **Контекст:** скачаны два официальных PDF. Оба оказались не буклетами базовой
  коробки, а полными сборниками. Новая вёрстка — 213 страниц, содержит Rule Omega и
  фракцию Daemon Sultan, то есть относится к эпохе Final Onslaught, и включает
  встроенный FAQ на с. 181–196.
- **Решение:** `SRC-RB-NEW` — основной источник PRIORITY 1. `SRC-RB-OLD` (254 с.) —
  контрольный: к нему обращаемся, когда формулировка в основном двусмысленна.
  Расхождение между вёрстками заводится как `SRC-###` (SOURCE-CONFLICT).
- **Следствия:** риск «рулбук старше эрраты» снят частично: рулбук из эпохи Final
  Onslaught, но `SRC-ERRATA-PACK` (12.2023) всё равно старше по приоритету при
  конфликте. Встроенный FAQ рулбука и отдельный `SRC-FAQ` (2019) пересекаются —
  сверяем оба.
- **Связанные issue:** —

## D-010 — Объём рулбука: ядро плюс модули, которые черновик реально использует

- **Дата:** 2026-09-10
- **Контекст:** нужно было определить, какие модули дополнений входят в наш рулбук
  (открытый вопрос с P1). Выведено из самого черновика, а не назначено.
- **Решение:** в рулбук входят ядро правил и те модули, которые черновик уже
  использует. Черновик объявляет три варианта игры (строки 61–66): Особые фабрики,
  Полковники, Уникальные Полковники, — и описывает в теле Наёмников, Лабораторию,
  Энграммы и Рой боевых дронов.
- **Соответствие оригиналу — подтверждено:**

  | Наш модуль | В оригинале |
  |---|---|
  | Полковник | High Priest |
  | Уникальные полковники | Unique High Priests (рулбук, с. 132) |
  | Наёмники: боевые машины, мехи, боевые роботы | Neutral Monsters + Independent Great Old Ones (у наёмного б/р есть карта лояльности = Loyalty Card) |

- **Соответствие остальных модулей — закрыто 2026-09-10** (направление задал Alek,
  каждое проверено по источнику):

  | Наш модуль | В оригинале | Где проверено |
  |---|---|---|
  | Особые фабрики | Colour Gates, дополнение The Colour Out of Space, 9 шт | `SRC-RB-NEW` стр. ~3174–3286 текста |
  | Лаборатория | The Laboratory, карта Yuggoth. Действие Surgery, стоимость 1 | `SRC-RB-NEW` ~4019–4038, вики Yuggoth Map |
  | Энграмма | Brain Cylinder. Особый Cultist, 4 на фракцию, стоимость 0, бой 0 | `SRC-RB-NEW` ~3996–4056, вики |
  | Рой боевых дронов | Slime Mold. Нейтральный монстр, 6 шт, стоимость 1 (0, если ни одного в игре), бой 2 | `SRC-RB-NEW` ~3975–4056, вики |
  | ЦУ / Узел управления | Область с жетоном No Slime: контроль фабрики в ней даёт карту лояльности Роя | вики Yuggoth Map, раздел Slime Molds |
  | Зоны сети | Области Slime Sea, куда можно призывать и где может ходить Рой | там же |

  Оригинал прямо предусматривает перенос обоих модулей на другие карты: жетоны
  Laboratory Token, Slime Token и No Slime Token существуют именно для этого. То есть
  наша адаптация Роя и Лаборатории к общей карте — не отклонение, а штатный режим.

- **Числа, подлежащие сверке в P5** (взяты из оригинала, в черновике не проверены):
  Colour Gates 9; Brain Cylinder 4 на фракцию, стоимость 0, бой 0; Surgery стоимость 1
  и заменяет до 4 Cultists; Slime Mold 6, стоимость 1 либо 0, бой 2.
- **Следствия:** варианты игры остаются вариантами и в нашем тексте (исключение из
  `D-003`), но подаются без указания на происхождение из дополнений.
- **Связанные issue:** —

## D-011 — Пять типов отрядов; «мех» — это Terror, Elder God — свойство боевого робота

- **Дата:** 2026-09-10
- **Контекст:** в глоссарии проекта «мех» не имел пары в оригинале. Рабочая гипотеза
  Alek — Elder God.
- **Решение по источнику:** гипотеза не подтвердилась, «мех» — это **Terror**.
  Рулбук, с. 103: Terror призывается как Monster (действие Summon Terror, требует
  контролируемых врат), равен Monster в захвате Cultists, но, будучи отдельным типом
  отряда, не уязвим к способностям, прицельно бьющим по Monsters. Это дословно наше
  «строится как боевая машина, но неуязвим к эффектам против БМ».
- **Elder God** — отдельный, пятый тип: во всём равен Great Old One, кроме двух
  пунктов (не даёт собственного Elder Sign для Ритуала; никогда не бросает боевые
  кубики, вместо этого даёт фиксированный эффект). Подтверждено Alek: Elder God есть
  у Bubastis, которая у нас Moon Systems. Значит у нас это **свойство отдельного
  боевого робота**, а не самостоятельная категория отрядов. Саму фракцию не выверяем
  (`D-004`), фиксируем только соответствие.
- **`Building` типом отряда в оригинале не является:** сплошной поиск по рулбуку даёт
  ноль вхождений. Врата, тайл Луны, жетоны осквернения и жетон Лаборатории —
  не отряды.
- **Итоговая таксономия:** Cultist (подтипы Acolyte, High Priest, Brain Cylinder),
  Monster, Great Old One (свойство Elder God), Terror.
- **Связанные issue:** —

## D-012 — Границы baseline и порядок работы с FAQ

- **Дата:** 2026-09-10
- **Контекст:** компендиум оригинала — 213 страниц, из которых ядро правил занимает
  40. Нужно было решить, что из остального попадает в эталон.
- **В baseline входит:** ядро правил; игра на двоих; правила модулей, которые
  использует наш черновик (полковники, уникальные полковники, наёмники,
  независимые боевые роботы, особые фабрики, лаборатория, энграммы, рой); общие
  правила типов отрядов.
- **В baseline не входит:**
  - фракционные правила, составы и способности — отдельная задача (`D-004`);
  - карты кроме земной: Dreamlands, Library at Celaeno, Primeval, Shaggai, Yuggoth
    целиком. Из Yuggoth взяты только Лаборатория, Энграммы и Рой — ровно те
    элементы, которые оригинал сам разрешает переносить на другие карты;
  - прочие модули раздела Mixed & Miscellaneous. Они попадут в baseline только
    если окажется, что черновик их использует; проверяется в P4.
- **Порядок работы с FAQ:** в baseline заводятся только те вопросы и ответы,
  которые меняют, ограничивают или снимают неоднозначность правила. Ответы, которые
  просто разбирают пример по уже записанному правилу, записей не создают — они
  пойдут в P8 как материал для формулировок. Источников FAQ два: встроенный
  в рулбук (с. 181–196) и отдельный `SRC-FAQ` от 2019 года; они сверяются между
  собой, расхождения заводятся как `SRC-###`.
- **Связанные issue:** —

## D-013 — Rule Omega записывается кратко, без риторики и отсылок

- **Дата:** 2026-09-10
- **Контекст:** оригинальное Rule Omega занимает пять предложений, из которых три —
  риторика, и ссылается на FAQ издателя и на «владельца игры».
- **Решение:** записать в одном абзаце: если ответа в правилах нет — голосуйте,
  голос владельца коробки весомее, хоумрульте свободно. Игра должна быть весёлой,
  а не поводом душнить над каждой буквой правил. Отсылки к FAQ издателя
  не переносятся.
- **Следствия:** механическая суть сохранена, раздел остаётся в рулбуке.
- **Связанные issue:** `RULE-002`

## D-014 — Особые фабрики и полковники: что идёт в правила, а что остаётся компонентом

- **Дата:** 2026-09-10
- **Контекст:** два места оригинала свёрстаны широкими таблицами, и извлечение их
  разваливает. Оказалось, что в нашем проекте они реализованы иначе, и таблицы
  в рулбуке вообще не нужны.
- **Особые фабрики.** Таблица эффектов по броску кубика у нас реализована
  **отдельными картами формата таро**. Это компонент, а не текст правил. В рулбук
  идёт только механика: как создаются особые фабрики, что светящуюся нельзя создать,
  не контролируя другую особую, что в фазу сбора Power они работают как обычные
  и что в начале фазы Doom бросается кубик. Сама таблица в рулбуке не печатается.
- **Полковники.** У нас полковники напечатаны **на планшетах фракций** вместе
  с пехотинцами, там же описано их свойство. В правилах их нужно только описать
  как тип отряда; таблица уникальных полковников в рулбук не идёт.
- **Следствия:** задача `CODE-02` потеряла смысл и отменена. Обе таблицы остаются
  в baseline как эталон для будущей сверки наших карт и планшетов с оригиналом —
  это отдельная задача, вне работы над правилами (`D-004`).
- **Связанные issue:** `RULE-001`

## D-015 — При конфликте официальных источников берём самый свежий

- **Дата:** 2026-09-10
- **Контекст:** нашлось первое расхождение двух официальных источников (`SRC-001`,
  формулировка про невидимость: «для этой цели» в FAQ 2019 года против «для любых
  целей» в рулбуке эпохи Final Onslaught).
- **Решение:** **действует всегда и без отдельного согласования** — при расхождении
  нескольких официальных источников берётся самый свежий. Спрашивать автора по
  каждому такому случаю не нужно.
- **Следствия:** issue `SRC-###` при конфликте всё равно заводится, но не как вопрос,
  а как фиксация: что разошлось, в чём, какой источник новее, что принято. Решение
  автора требуется только если свежесть источников одинакова или неизвестна.
  Правило внесено в `RULES-CHARTER.md` §5 вместе с порядком свежести наших
  источников.
- **Связанные issue:** `SRC-001`

## D-016 — Термины результатов боя `ОТМЕНЕНО, СМ. D-017`

> Отменено 2026-09-10 в части Kill и Elimination. «Подавление» под Pain сохраняется
> и подтверждено Alek. Разбор ошибки — в `D-017`.

- **Дата:** 2026-09-10
- **Контекст:** Alek поручил проанализировать связку «смерть / уничтожение /
  ликвидация», выбрать лучшие варианты и точно развести значения, разрешив менять
  и термины, и их значения. Отдельно предложено подобрать тематичную альтернативу
  для «отступления».
- **Проблема исходной связки.** В оригинале три разных понятия. Kill — результат
  броска 6 в бою. Elimination — удаление отряда с поля любым другим способом.
  Pain — результат броска 4–5. Исход у Kill и Elimination одинаковый: отряд
  возвращается в запас. Но десятки способностей срабатывают от Kill и НЕ срабатывают
  от Elimination, поэтому смешивать их нельзя. В черновике Kill = «смерть»,
  Elimination = «уничтожение», плюс в одном месте всплывает «ликвидировать».
  Беда в том, что «уничтожить» — это ещё и обычный русский глагол для «убрать
  отряд с поля», и он неизбежно течёт: черновик уже пишет «уничтожьте до 4 ваших
  пехотинцев» в значении, не имеющем отношения к результату боя.
- **Решение:**

  | Оригинал | Было | Стало |
  |---|---|---|
  | Kill | Смерть | **Уничтожение** |
  | Elimination | Уничтожение / Ликвидация | **Списание** |
  | Pain | Отступление | **Подавление** (применяется отступлением) |

- **Почему так:**
  - **«Смерть» уходит.** У нас воюют боевые роботы, боевые машины, мехи и рои
    дронов. Машина не умирает. «Уничтожение» одинаково работает и для техники,
    и для пехоты.
  - **«Списание» занимает место Elimination.** Термин корпоративно-промышленный,
    ровно в сеттинге: корпорация не «уничтожает» актив, а списывает его. Главное —
    это НЕ обычный глагол, поэтому течь ему некуда. Как следствие освобождаются
    нейтральные обороты «убрать с поля» и «вернуть в запас» — ими и пишется всё
    общее.
  - **«Подавление» разводит результат и его применение.** В оригинале Pain —
    назначаемый результат, а отступление — его применение; «отступление» в обеих
    ролях эту границу стирает. Подавление — стандартный военный термин, он
    называет состояние, а не движение: «назначить подавление», «подавленный отряд
    отступает». Слово уже звучит во вступлении к сеттингу. Отдельно это чинит
    формулировку про энграммы: им нельзя назначить подавление, но чужому
    отступлению в свой регион они не мешают.
- **Следствия:** отменено, см. `D-017`.
- **Связанные issue:** `TISS-001`

## D-017 — Термины результатов боя `ЧАСТИЧНО ОТМЕНЕНО, СМ. D-018`

> Отменено 2026-09-10 в части Elimination: «ликвидация» заменена обратно
> на «уничтожение». «Гибель» под Kill и «подавление» под Pain сохраняются.
> Причина отмены — в `D-018`: диагноз «течи» оказался ложным.

- **Дата:** 2026-09-10
- **Контекст:** `D-016` отвергнуто Alek по двум основаниям, оба справедливы.
  «Списание» звучит бухгалтерией, а не исходом сражения. И главное — решение
  внутренне противоречиво: аргумент «уничтожить» — обычный глагол, поэтому он
  течёт» был применён к Elimination и тут же проигнорирован, когда «уничтожение»
  ставилось на Kill. Слово остаётся обычным глаголом независимо от того, к какому
  из двух понятий его прикрепить.
- **Разбор ошибки.** Течь возникла именно на стороне Elimination: черновик пишет
  «уничтожьте до 4 ваших пехотинцев», имея в виду плату за действие, а не результат
  боя. Слово «смерть», которое стояло на Kill, при этом не текло никуда: никто
  не напишет «умертвите 4 своих пехотинцев» в значении платы. То есть исходная
  связка была наполовину здоровой, и чинить нужно было только больную половину.
  Ошибка `D-016` — в том, что вместо точечной починки термины были переставлены
  местами по слабому аргументу «машина не умирает».
- **Решение:**

  | Оригинал | Стало | Почему |
  |---|---|---|
  | Kill | **Гибель** | Результат броска 6. Не глагол платы: «погубите 4 своих пехотинцев» никто не напишет. Работает и для пехоты, и для техники — «машина погибла» по-русски нормально. Register эпического сражения, а не отчётности |
  | Elimination | **Ликвидация** | Удаление любым иным способом. Несёт умысел: ликвидируют намеренно, решением или способностью, — ровно то, чем Elimination отличается от гибели в бою. Слово уже есть в словаре черновика (строка 764) |
  | Pain | **Подавление** | Без изменений, подтверждено Alek. Применяется отступлением |

- **Смысловая логика связки:** гибель — что случилось; ликвидация — что сделали.
  Различие интуитивное, а это важно: игрок обязан помнить, что способности,
  срабатывающие от гибели, не срабатывают от ликвидации.
- **«Уничтожение» выводится из терминологии рулбука совсем.** Как термин оно
  не используется ни для одного из двух понятий, а как обычный глагол не
  используется тоже — слишком близко по смыслу к обоим, будет путать. Для
  нейтральных случаев остаются обороты «убрать с поля» и «вернуть в запас».
- **Следствия:** частично отменено, см. `D-018`.
- **Связанные issue:** `TISS-001`

## D-018 — Термины результатов боя `ЧАСТИЧНО ОТМЕНЕНО, СМ. D-019`

> Отменено 2026-09-10 в части Kill: «гибель» возвращена к «смерти». Разбор
> отношения Kill и Elimination, собранный здесь, остаётся в силе — он верен
> и лёг в `BL-COMBAT-009` и `BL-COMBAT-017`.

- **Дата:** 2026-09-10
- **Контекст:** Alek указал, что кубики дают только Kill и Pain, тогда как
  Elimination возникает от свойств, действий и положений на поле, и предположил,
  что Kill является частным случаем Elimination, а значит «уничтожение» подходит
  под Elimination как более широкое. Оба утверждения потребовали проверки
  по источникам, и проверка изменила решение.
- **Что подтвердилось.** Кубики действительно дают только Kill и Pain. Elimination
  кубиками не выпадает: он возникает от способностей, действий, оплаты и особых
  положений, а в бою добавляется способностями после боя как третий тип результата.
- **Вложенность есть, и она односторонняя.** Kill входит в Elimination как частный
  случай. Решающее свидетельство — текст карты Passion в Ultimate Errata Pack,
  самом свежем официальном источнике: «When one or more of your Cultists are
  **Eliminated by an enemy (Killed, Captured, etc.)**». Карта раскрывает Elimination
  перечислением, где Killed стоит первым пунктом. Подтверждается двумя ответами
  FAQ: Passion срабатывает и при Eradication, и при захвате или обращении Brain
  Cylinder. Обратное направление не работает, и это тоже сказано прямо:
  «Cannibalism требует Killed, а не Eliminated», «Unholy Ground даёт Elimination,
  а не Kill».
  **Промежуточно я пришёл к противоположному выводу и был неправ.** Формулу
  «Killed or Eliminated» с карт Yothan и Bloated Woman и из правил игры на двоих
  я прочитал как доказательство непересечения — на деле это усиление ради
  однозначности. Ошибка исправлена в `BL-COMBAT-009` и `BL-COMBAT-017`.
- **Отдельный слой, который нельзя терять.** В бою Elimination — это ещё и
  самостоятельный третий тип назначаемого результата: способности после боя могут
  его добавить, и применяется он раньше Kill. В оригинале одно слово несёт оба
  смысла — и состояние «убран с поля», и этот тип результата. Мы обязаны
  воспроизвести это как есть, но в глоссарии сказать явно.
- **Моя ошибка, которую эта проверка вскрыла.** В `D-016` и `D-017` я утверждал,
  что слово «уничтожить» в черновике «течёт» в общее значение, и строил на этом
  всю перестановку терминов. Сплошной поиск показал: в черновике всего пять
  вхождений «уничтож*», и течи среди них нет. Строка 650, «уничтожьте до 4 ваших
  пехотинцев», — это дословный и правильный перевод «Eliminate up to 4 of your
  Cultists» из описания Лаборатории, то есть корректное употребление термина,
  а не общее слово. Диагноз был ложноположительным, и оба решения на нём
  оказались лишними.
- **Решение:**

  | Оригинал | Термин | Чем задаётся |
  |---|---|---|
  | Kill | **Гибель** | Только результат броска 6 в бою |
  | Elimination | **Уничтожение** | Способности, действия, оплата, положение на поле; в бою — как третий тип результата от способностей после боя |
  | Pain | **Подавление** | Результат броска 4–5, применяется отступлением |

  «Уничтожение» возвращается на Elimination — то есть остаётся тем, чем было
  в черновике. Меняется только «смерть» на «гибель»: слово одинаково работает
  для пехоты и для техники, а машина не умирает. «Ликвидация» и «списание»
  не используются.
- **Требование к формулировкам:** вложенность обязана читаться из самих слов, и
  пара «гибель — уничтожение» это обеспечивает: **погибший отряд уничтожен,
  но не всякий уничтоженный погиб.** Эту фразу имеет смысл поставить в глоссарий
  дословно — она снимает вопрос за одну строку.
  Проверка при вычитке каждой способности: сформулирована она через гибель или
  через уничтожение? Через гибель — срабатывает только от броска 6. Через
  уничтожение — срабатывает от любого удаления с поля, включая гибель.
- **Следствия:** в части Kill отменено, см. `D-019`.
- **Связанные issue:** `TISS-001`, `DEV-001`

## D-020 — Решения по первым пяти расхождениям

- **Дата:** 2026-09-10
- **`DEV-003`, особые события фазы влияния — ПРАВИМ, приводим к оригиналу.**
  Убирается фраза «после того, как все игроки приняли решение об оказании
  давления». Возвращается оригинальный тайминг: особые события срабатывают
  в очередь игрока на оказание давления, независимо от того, оказывает он
  давление или нет.
  Уточнение по ходу проверки: **четырёхэтапную подачу фазы менять не нужно.**
  Карта-памятка из Ultimate Errata Pack сама перечисляет фазу влияния тремя
  пунктами, где особые события идут последним. То есть структура подачи
  официальна, ломает механику только добавленная фраза про «после всех».
- **`DEV-004`, первый игрок в начале партии — ПРАВИМ частично.**
  «Рекомендуется» возвращается к «всегда». Проверено по требованию Alek:
  ни эррата, ни дополнения этого правила не меняли. Карта-памятка Ultimate
  Errata Pack описывает фазу определения первого игрока без оговорок, а обе
  вёрстки рулбука дают одну и ту же формулировку «always the First Player
  at the start of the game».
  Оговорка про фракцию, которая никогда не может быть первым игроком в начале
  партии, **не переносится**: в оригинале это Windwalker, а у нас такой фракции
  нет. Если она когда-нибудь появится, правило придётся вернуть — отмечено
  в issue.
- **`DEV-001`, наёмный боевой робот — ФИКСИРУЕМ.** «Уничтожен» меняется
  на «убит», заголовок раздела — на «Смерть». Сплошная сверка фракций, карт,
  планшетов и прочих компонентов на соответствие терминологии и оригинальным
  текстам — отдельная задача после правил.
- **`DEV-005`, трек размечен до 25 — ПРИНИМАЕМ как есть.** 20 не является
  пределом и в оригинале: при превышении добавляется второй маркер. Наши 25 —
  тот же приём с небольшим запасом. На игровое поведение не влияет.
- **`DEV-006`, стоимость боевой машины 1–3 — ОТЛОЖЕНО.** Проверяется на самих
  компонентах при выверке фракций, не здесь.
- **Связанные issue:** `DEV-001`, `DEV-003`, `DEV-004`, `DEV-005`, `DEV-006`

## D-021 — Состав игроков 3+; способности юнитов не расписываются в правилах

- **Дата:** 2026-09-10
- **`MISS-001` закрыт: потери нет.** Способность полковника у нас существует
  и называется **«Боевое донесение»**. Она пропечатана на планшете каждой
  фракции. В оригинале полковники были дополнением, поэтому на планшетах их
  не было и правило расписывалось в рулбуке; у нас они включены изначально,
  как и прочие свойства юнитов и фракций.
- **Отсюда общее правило подачи.** Свойства конкретных юнитов и фракций
  **в книге правил детально не расписываются** — они на планшетах. Рулбук
  описывает типы отрядов и общие правила; конкретные способности живут на
  компонентах. Детальный перечень уместен в отдельной таблице или реестре
  фракций и юнитов, но не в тексте правил.
  Это меняет то, как читать все находки вида «в правилах нет способности X»:
  прежде чем заводить `MISSING`, надо проверить планшет.
- **`DEV-007` закрыт: игра на двоих исключается.** Целевой состав — **от 3 до 5
  игроков**. Больше приемлемо, правила на 6–8 ведём, хотя компонентов пока может
  не хватать. Игра на двоих не нужна и не будет.
  Следствия: оригинальный режим «один на один» с маркером Распада в наш рулбук
  не переносится и из baseline выводится за объём; оба варианта черновика,
  где игрок водит две или три фракции (строки 554–575), из текста удаляются;
  в подготовке убирается «(или два)» из заголовка «Четыре (или два) игрока».
- **Опечатки и остатки перевода правятся молча.** Строка 417 «Стоимость: 0 Силы»
  и подобное — это не вопрос к автору, а работа редактора. Уточнение
  к `D-007`: issue заводится там, где затронута механика; чистая опечатка
  просто исправляется.
- **Связанные issue:** `MISS-001`, `DEV-007`, `TISS-009` (ссылка поправлена
  при фризе P7: `TERM-060` был записью карты терминов, а не issue; дефект
  «Стоимость: 0 Силы» живёт теперь как `TISS-009`)

## D-022 — Правки по итогам P4; отдельного FAQ не будет

- **Дата:** 2026-09-10
- **`DEV-016` — правим, приводим к оригиналу.** Если до бросков удалены все
  отряды одной из сторон, битва окончена и дальнейшие способности
  не применяются. Часть ответа черновика про потраченную нефть сохраняем —
  она оригиналу не противоречит и снимает реальный вопрос.
- **`DEV-011` — правим, фиксируем.** Применение любой способности необязательно,
  если в её тексте прямо не сказано обратное, и это относится в том числе
  к постоянным эффектам. Обязательность пишется явно.
- **`DEV-013` — исправляем.** Полковник является пехотинцем и, как любой
  пехотинец, приносит 1 нефть. Слово «дополнительную» убирается.
- **`DEV-015` — правим, проверено повторно.** Привязки к фазе в оригинале нет:
  «If this Gate becomes Controlled by another Faction, all Slime Molds in play
  change their allegiance to the Gate's new Controller». Карта Контроля сети
  и контроль над Роем переходят в момент взятия узловой фабрики под контроль,
  в любой фазе.
- **`MISS-015` — недоразумение снято.** Соответствие «мех = Terror» верное
  и под сомнение не ставилось. Проблема была уже: определение отряда
  в глоссарии черновика (строка 781) перечисляет только пехотинца, боевую
  машину и боевого робота — мех в список не попал. Формально мех тогда
  не отряд, и правила, написанные через «отряд», на него не действуют.
  Правка: в определение добавляется мех, термин «боевая единица» из правил
  убирается целиком в пользу «отряда».
- **Потерянные правила возвращаем.** Все 16 находок типа `MISSING` подлежат
  восстановлению.
- **ОТДЕЛЬНОГО FAQ НЕ БУДЕТ.** Всё важное вписывается в основные правила
  в соответствующие разделы и формулируется так, чтобы вопрос не возникал.
  FAQ по определению — ответы на то, что правила не закрыли; мы закрываем сразу.
  Следствия: три правила, живущие сейчас в FAQ черновика (`MISS-007`,
  `MISS-012`, `DEV-016`), переносятся в тело правил; раздел FAQ из черновика
  удаляется целиком; каждый вопрос удаляемого FAQ перед удалением проверяется
  на то, закрыт ли он теперь текстом правил.
- **Связанные issue:** `DEV-011`, `DEV-013`, `DEV-015`, `DEV-016`, `MISS-015`,
  и весь класс `MISSING`

## D-023 — «Баланс сил» — это Handicap Acolytes, перенос точный

- **Дата:** 2026-09-10
- **Контекст:** `RULE-006` — единственный кусок черновика, для которого не было
  опоры в baseline.
- **Найдено:** это модуль оригинала **Handicap Acolytes** (новая вёрстка с. 134,
  старая с. 155–156), 9 нейтральных серых Acolytes. Записан как `BL-MODULE-017`.
- **Сверка:** перенос точный. Совпадают оба случая, все числа и логика:
  сильному игроку один культист заменяется особым, который при попадании
  в резерв уходит в коробку и оставляет игрока с пятью; слабому добавляется один
  до семи. Совпадает даже вводная часть про разницу в опыте игроков.
- **Единственное отличие — компонентное и осознанное:** в оригинале это
  специальные серые культисты, у нас — рекрут любой неучаствующей фракции.
  Механически эквивалентно, отдельного решения не требует. Если серые фигурки
  когда-нибудь появятся, текст менять не придётся.
- **Дополнено `D-040` (2026-09-15):** носитель стал не один. Кроме рекрута
  неучаствующей фракции годится **фигурка энграммы** — в §11.7 она названа
  фишкой-заменителем и считается обычным рекрутом; правила энграмм (11.5)
  к ней не относятся. Эквивалентность оригиналу от этого не меняется, поэтому
  `D-023` остаётся в силе целиком.
- **Связанные issue:** `RULE-006`

## D-024 — Батч B1: «резерв» для фигурок, числа регионов печатаем

- **Дата:** 2026-09-10
- **Контекст:** первый батч P5. Два открытых вопроса по подготовке и раунду.
- **`TISS-002` — фигурки лежат в РЕЗЕРВЕ.** Слово «запас» в черновике означало
  сразу две вещи: место, где лежат неиспользуемые фигурки и технологии, и сам
  ресурс. Решение: отряды и технологии лежат в **резерве** фракции, нефть
  отслеживается на **треке запаса нефти**. Голое «запас» применительно
  к фигуркам из правил убирается сплошным поиском.
  Компонентный след: «из своего запаса» стоит примерно в десяти местах —
  карты технологий, уникальные полковники, карта событий фабрик. Карты
  собираются из HTML/JSON-шаблонов, поэтому это правка строк, а не перепечатка;
  уходит в сверку компонентов (`D-004`). Один компонент уже пишет «из вашего
  резерва», так что решение попутно чинит существующий разнобой. Планшет
  фракции печатает «Запас нефти:» — остаётся как есть.
- **`DEV-008` — закрыт целиком, тремя частями.**
  1. «(или два)» из заголовка раскладки на четверых убирается по `D-021`.
  2. **Раскладки на 6–8 сверены, расхождения нет.** `SRC-RB-NEW`, стр. 184,
     раздел «6-8 Player Maps»: на шесть игроков обе половины стороной «6»,
     на восемь — обе стороной «8», на семь — одна «6» и одна «8». Черновик
     (строки 131–136) повторяет дословно. Числа регионов для 6–8 оригинал
     не называет, и мы не называем. Занесено в `BL-SETUP-001`.
  3. **Числа регионов для 3–5 печатаем: 13, 17, 21.** Нарезка нашей карты
     совпадает с оригинальной — подтверждено Alek. Числа идут в раздел
     подготовки как проверка правильности сборки поля.
- **Следствия:** батч B1 закрыт, открытых вопросов по подготовке и раунду нет.
  «Резерв» фиксируется в `terms.yaml` при фризе терминологии в P7.
- **Связанные issue:** `TISS-002`, `DEV-008`

## D-025 — Батч B2: лимиты «за действие», кроме перемещения; отсчёт от активного игрока

- **Дата:** 2026-09-10
- **Контекст:** второй батч P5, область «действия».
- **`DEV-009` — обе части к оригиналу, но предложение issue пришлось поправить.**
  - Часть (а): «в начале вашего хода» убирается. Условие «если у вас нет отрядов
    на поле» проверяется в момент вербовки (`BL-ACTION-009`).
  - Часть (б): «за ход» → «за это действие» **ровно в четырёх местах**, каждое
    сверено с baseline: вербовка (строка 329, `BL-ACTION-009`), производство
    боевой машины (337, `BL-ACTION-010`), строительство фабрики (350,
    `BL-ACTION-012`), битва (438, `BL-ACTION-014`).
  - **Исключение: перемещение, строка 367 — не трогаем.** Исходное предложение
    issue говорило «везде», и это было бы ошибкой. В оригинале
    (`BL-ACTION-013`) там действительно **за ход**: одним действием Move можно
    двигать сколько угодно отрядов, но каждый отряд не более одного раза за ход.
    Черновик здесь точен.
  - Лимиты, которых в черновике нет вовсе (`MISS-004` создание боевого робота,
    `MISS-006` захват), дописываются сразу в форме «за одно действие» по `D-022`.
- **`RULE-003` — берём формулировку оригинала.** По требованию Alek сначала
  проверено по источникам: правило нашлось в **обеих** вёрстках рулбука дословно
  и одинаково (`SRC-RB-NEW` строки 695–698, `SRC-RB-OLD` строки 872–873), поэтому
  идти на форумы и в базы не понадобилось и конфликта источников нет.
  Наш текст: «Если такую способность хотят применить несколько игроков, порядок
  начинается с игрока, чьё действие только что завершилось, и далее идёт
  по порядку хода.»
  Попутно в той же колонке подтвердилось второе правило `MISS-008`: при равных
  претендентах на незанятую фабрику её получает тот, чей ход наступит первым.
- **Следствия:** батч B2 закрыт. Остальные issue области «действия» решений
  не требуют: `MISS-002`, `MISS-003`, `MISS-004`, `MISS-007`, `MISS-008` идут
  по `D-022`, `DEV-006` ждёт выверки компонентов.
- **Связанные issue:** `DEV-009`, `RULE-003`

## D-026 — Батч B3: два такта финальной фазы; фракция вне битвы — только финальным эффектом

- **Дата:** 2026-09-10
- **Контекст:** третий батч P5, область «бой».
- **`DEV-010` — трёхфазная подача сохраняется, финальная фаза делится на два
  такта.** Названия «тактическая подготовка / сражение / финал» остаются:
  термины «тактический эффект» и «финальный эффект» уже напечатаны на картах
  технологий и полковниках. Внутри финальной фазы явно выделяются два такта:
  - **такт 1, назначение** — сначала смерти и уничтожения (одновременно), затем
    подавления; в этот момент работают финальные эффекты, меняющие результаты
    и порядок их назначения;
  - **такт 2, применение** — обе стороны сначала убирают уничтоженные отряды,
    затем убитые, одновременно, и только потом применяются подавления.

  Опора, `SRC-RB-NEW` стр. 29: «Kills and Eliminations must be applied before
  Pains. Both sides remove their Eliminated Units, and then their Killed units,
  all at the same time.»
  Попутно найдено правило, которого в черновике нет и которое дописывается сюда
  же: отступления применяет сначала атакующий, затем защищающийся.
- **`EXTRA-001` — переклассифицирован и переписан.** Был заведён как безобидное
  добавление (`EXTRA`/`WARNING`); прямой текст рулбука показал, что это
  расхождение (`DEVIATION`/`ERROR`), причём двойное.
  - **Что говорит оригинал.** `SRC-RB-NEW`, стр. 29: «There are a few Post-Battle
    abilities that can be used even when you are not involved in the Battle.
    **This is the only way** that a Faction not directly involved in a Battle can
    participate in that battle.» Неучаствующая фракция вмешивается только
    финальным эффектом и никогда — в тактической фазе. Момент задаётся текстом
    самой способности (Cannibalism — после применения всех результатов;
    Necrophagy — при назначении; Madness — при применении).
  - **Что не закрыто нигде.** Порядок между несколькими неучаствующими игроками
    отсутствует и в рулбуке, и в Ultimate Errata Pack, и в FAQ — проверено
    сплошным поиском.
  - **Решение.** Строка 465 переписывается тремя утверждениями: (1) фракция вне
    битвы вмешивается только финальным эффектом, в тактическую подготовку —
    никогда; (2) момент задаёт текст эффекта; (3) если на один момент претендуют
    несколько игроков, порядок начинается с атакующего и далее по порядку хода.
  - **Третий пункт — наше дополнение,** и это отмечено сознательно: в оригинале
    правила нет. Форма взята по образцу самого оригинала, где тот же отсчёт
    работает трижды — спор прерывающих способностей (`D-025`), порядок
    способностей до боя, порядок применения подавлений. Везде первым идёт
    атакующий.
- **Следствия:** батч B3 закрыт. Остальные issue области «бой» решений
  не требуют: `MISS-005`, `MISS-006`, `MISS-009`, `MISS-010`, `MISS-011` идут
  по `D-022`, `DEV-016` уже решён там же.
- **Связанные issue:** `DEV-010`, `EXTRA-001`

## D-027 — Батч B7: игра заканчивается по завершении текущего хода

- **Дата:** 2026-09-10
- **Контекст:** четвёртый батч P5, область «победа и конец игры». Единственный
  открытый вопрос — `DEV-012`.
- **Решение: приводим к оригиналу.** Черновик обрывал партию «немедленно»
  в момент раскрытия скрытого влияния. Оригинал (`SRC-RB-NEW` стр. 42) даёт
  другой момент: «the game will end when the current player's turn ends (even
  if it is not the turn of the player who revealed Elder Signs to go over
  30 Doom)».
  Наш текст: игра заканчивается по завершении хода текущего игрока — даже если
  это не ход того, кто раскрыл карты. Уточнение в скобках переносится: без него
  правило прочтут как «конец хода того, кто раскрыл».
- **Практическое следствие:** обрывать чужое действие раскрытием карт нельзя.
  Активный игрок доигрывает ход — завершает начатое действие, доводит бой,
  применяет способности, — и это может изменить исход партии.
- **Попутное уточнение из того же абзаца:** «No matter how the end of the game
  was triggered, all players may reveal their Elder Signs before the victor is
  declared.» Раскрыть карты перед объявлением победителя могут все игроки и при
  любом способе завершения партии. Фраза черновика «больше никаких ходов они
  не выполняют» согласовывается с тем, что текущий ход доигрывается.
- **Связанные issue:** `DEV-012`

## D-028 — Приёмка CODE-03: префикс `TISS-###`, полковников одиннадцать, приложение устарело

- **Дата:** 2026-09-10
- **Контекст:** приёмка сплошного numerical audit. Задача принята; две находки
  потребовали решения, и одна из них оказалась крупнее, чем выглядела.
- **`TERM-###` разведён на два префикса.** Устав §9 отдавал этот префикс записям
  карты терминов и не называл его среди префиксов issue, но в `issues.yaml` уже
  жили `TERM-001` и `TERM-002` — валидатор считал их дублями.
  **Решение: issue типа `TERMINOLOGY` получают префикс `TISS-###`.**
  `TERM-###` остаётся только за `terms.yaml`. Переименованы `TERM-001` → `TISS-001`
  и `TERM-002` → `TISS-002`; ссылки поправлены в `DECISIONS.md`, `numbers.yaml`
  и `redesign.yaml`. Ссылки `refs:` внутри `terms.yaml` не трогались — они
  указывают на записи терминов, а не на issue.
  Заодно в §9 записано то, что и так подразумевалось: идентификатор уникален
  внутри своего реестра, а не глобально.
- **Уникальных полковников одиннадцать, и `DEV-018` закрыт в день заведения.**
  Число подтверждено Alek и проверено по `Templates/UniqueColonel/uc.md`, где
  ровно одиннадцать пронумерованных карт. Строка 67 черновика верна.
  Расхождение с оригиналом (8) расхождением не считается: число уникальных
  полковников задаётся составом нашего проекта, а не механикой Cthulhu Wars,
  ровно как число фракций.
- **Настоящая находка оказалась другой: в проекте два несовместимых набора
  полковников** (`SRC-002`). Приложение
  `Drafts/…Уникальные полковники.pdf` описывает десять полковников — Оберст
  Херцог, Джеймс Брэддок и далее, — и ни одно имя не пересекается с набором
  из `Templates/UniqueColonel/`. Это не две редакции одного набора, а два разных.
  Опознаётся однозначно: в приложении названия способностей оставлены
  по-английски и совпадают с оригинальными Unique High Priests один в один
  (The Thing from Beyond, The Ultra-Violet, The Reanimator Serum, Beyond the
  Spheres, Daemon Heroine, The Bride, The Pit of Despair, Eldritch Wisdom,
  True Vision). То есть приложение — рабочий перевод оригинала, а `Templates/` —
  готовый реcкин.
  **Решение: приложение переводится в архив**, как PDF правил по `D-001`.
  Эталон по уникальным полковникам — `Templates/UniqueColonel/`.
- **Из этого выведено общее правило и внесено в устав §5:** если по одной
  и той же сущности есть файл в `Drafts/` и живой генератор в `Templates/`,
  эталон — генератор. `Drafts/` содержит рабочие материалы разной свежести,
  `Templates/` — то, что реально печатается.
- **Следствия:** на текст правил не влияет — по `D-014` таблица уникальных
  полковников в рулбук не идёт. Записи `NUM-113` и `NUM-114` пересмотрены:
  первая переведена в `PASS`, вторая помечена как снятая с устаревшего
  источника. При выверке компонентов проверить, нет ли такой же подмены
  в двух других приложениях `Drafts/` — особые фабрики и наёмники, — и развести
  совпадение имён: способность полковника Генри Брута называется «Извлечение
  сознания» так же, как действие Лаборатории.
- **Попутно закрыт пункт 14 отчёта `CODE-03`.** Code заметил, что приложение
  противоречит черновику по двум пунктам, и не стал заводить issue, потому что
  это не числа. Проверка по оригиналу показала, что прав **черновик**, а
  приложение расходится и с ним, и с оригиналом: ограничения «не могут строить
  и контролировать фабрики» в разделе Unique High Priests нет вовсе, а про наём
  оригинал говорит прямо обратное — «once a player has Recruited a Unique High
  Priest, no other player may Recruit that particular Priest». В нашем
  действующем наборе «любой игрок» уцелело как именная способность одного
  полковника, Генри Брута, и там прямо помечено как исключение. Приложение
  обобщило исключение в правило — ещё одно доказательство его устарелости.
  Текст правил не трогаем: строки 586 и 600 черновика верны.
- **Связанные issue:** `DEV-018`, `SRC-002`, `TISS-001`, `TISS-002`

## D-019 — ФИНАЛ по терминам результатов боя: смерть, уничтожение, подавление

- **Дата:** 2026-09-10
- **Контекст:** «гибель» отвергнута Alek как поэтичный синоним смерти, который
  не ложится. Запрошены звучные и тематичные варианты, при отсутствии которых
  остаётся «смерть».
- **Решение:**

  | Оригинал | Термин | Чем задаётся |
  |---|---|---|
  | Kill | **Смерть** | Только результат броска 6 в бою |
  | Elimination | **Уничтожение** | Любое удаление с поля, включая смерть; в бою — ещё и третий тип результата от способностей после боя |
  | Pain | **Подавление** | Результат броска 4–5, применяется отступлением |

  Формула для глоссария: **убитый отряд уничтожен, но не всякий уничтоженный убит.**
- **Почему «смерть» устояла после трёх попыток её заменить.** Требование к паре —
  чтобы вложенность читалась из самих слов. Для этого термины обязаны лежать
  в РАЗНЫХ смысловых полях. «Смерть» и «уничтожение» — разные поля, и формула
  выше очевидна без пояснений. Все рассмотренные замены — гибель, ликвидация,
  устранение, списание — лежат в одном поле с уничтожением, и вложенность в паре
  с ними не читается вовсе. Второе: «смерть» не может утечь в общее значение,
  потому что «умертвите 4 своих пехотинцев» никто не напишет.
- **Что было рассмотрено и отклонено:**
  - **Поражение** — военный термин поражения цели, работает и для пехоты,
    и для техники, вложенность читается. Отклонено из-за омонимии: «поражение
    в бою» читается как проигрыш сражения, а это ловушка в тексте про битвы.
  - **Потеря** — «за каждую шестёрку назначается потеря» звучит хорошо, но нет
    чистой глагольной формы: способности формулируются как «когда отряд убит»,
    а «когда отряд потерян» двусмысленно.
  - **Гибель, ликвидация, устранение, списание** — см. выше и `D-016`, `D-017`.
- **Известная слабость решения:** для боевых роботов, машин, мехов и роя дронов
  «смерть» звучит небезупречно. Принято как меньшее зло; глагольная форма
  «отряд убит» снимает большую часть неловкости.
- **Следствия:** терминология результатов боя в черновике меняется только
  в одном месте — «отступление» как название результата становится «подавлением».
  «Смерть» и «уничтожение» остаются как есть. Плюс точечно правится `DEV-001`
  и единственный стихийный вариант «Ликвидировать» в строке 764.
- **Связанные issue:** `TISS-001`, `DEV-001`

## D-035 — Решения по механическому diff P9

- **Дата:** 2026-09-11
- **`DEV-002` переоткрыт и закрыт заново — по оригиналу.** Моё закрытие в P4
  было неверным: я сверил арифметику 6 + 2 = 8 и не учёл, что §4.1 велит
  применять в фазу ресурсов фракционные способности добычи. Фракция
  со способностью входила бы в первую фазу действий больше чем с восемью
  нефтями, чего в оригинале нет.
  **Решение:** максимально близко к оригиналу. Стартовые 8 нефти выдаются
  **при подготовке** (§2.6), а **первый раунд состоит только из фазы
  действий** — первые три фазы в нём не играются.
  Проверка эквивалентности: последовательность сыгранных фаз получается
  ровно оригинальной, меняются только границы раундов. Оригинал: действия →
  ресурсы → первый игрок → влияние | действия → … У нас: действия | ресурсы →
  первый игрок → влияние → действия | … Та же цепочка.
  Побочно снят абзац §5.1 про «в первом раунде запас у всех одинаков» — фаза
  в первом раунде просто не играется.
- **`A2`, стоимость карт лояльности наёмников — вопрос снят.** Влияние
  не потеряно: оно входит в стоимость, напечатанную на самой карте.
  **Запомнить при вёрстке карт наёмников:** цена включает и влияние, и нефть.
  В текст правил число не выносится.
- **`A3`, стоимость вербовки полковника — в книге не дублируется.** Она
  на планшете. Правка только редакторская: §7.3.1 больше не утверждает
  «Стоимость: 1 нефть» безусловно, а говорит «1 нефть за рекрута» и отсылает
  к планшету для остальных типов пехотинцев. Конфликт §7.3.1 ↔ §11.2 снят.
- **`A4`, карта события вместо броска кубика — подтверждено.** Карта каждый раз
  **возвращается в колоду, и колода тасуется**, поэтому распределение исходов
  совпадает с шестигранным кубиком. Уточнение про возврат и перетасовку должно
  стоять в тексте §11.1 явно — без него равновероятность не гарантирована.
- **Противоречие «наёмники и технологии» снято как ложное.** Alek прав: наёмник
  не зависит от ФРАКЦИОННЫХ технологий, а собственную технологию наёмного
  боевого робота даёт его карта лояльности. Это разные вещи. Правка
  редакторская: в §11.4 добавлено слово «фракционных» и прямая отсылка
  к 11.4.3.
- **Свободное действие при нулевом остатке — правило уточнено по оригиналу.**
  Baseline `BL-ACTION-003`: «если **на момент своего хода** игрок не имеет
  Power, его ход пропускается». Проверка разовая, в начале хода. Потратив
  последнюю нефть в свой ход, игрок свободных действий не теряет. Интуиция
  Alek подтвердилась источником: построив фабрику последними тремя нефтями,
  встать на неё можно. В §7.6 добавлен явный абзац с этим примером.
- **`D-033` отменено этим решением.** Оно велело оставить правила как есть,
  а строку «Запас нефти: 8» убрать **с планшета**. Теперь верно обратное: 8
  выдаются при подготовке, и строка на планшете правильна. Поручение править
  восемь `Templates/Saved/*.json` отзывается — при выверке компонентов проверить,
  что оно не было выполнено, и если было, вернуть строку.
- **Связанные issue:** `DEV-002`, `DEV-014`

## D-036 — Урок P9: правка правила требует сплошного поиска по книге

- **Дата:** 2026-09-11
- **Что произошло.** Восемь правок по итогам `D-035` были внесены точно
  по эталону — и четыре из них создали НОВЫЕ противоречия, потому что я правил
  то место, которое нашла проверка, и не проходил сплошным поиском по всем
  остальным упоминаниям того же правила.
  Конкретно: 8 нефти вписаны в §2.6, но §2.4 остался с нулём; победитель
  переписан в §9.4, но §3.1 сохранил прежнюю дыру; цена вербовки уточнена
  в §7.3.1, но сводная таблица §7.3 осталась со старым числом; проверка нуля
  нефти уточнена в §7.6, а головное правило §7.1 продолжало учить прежнему.
- **Правило на будущее:** правка любого правила считается законченной только
  после сплошного поиска по обоим файлам — телу правил и глоссарию — по всем
  словам этого правила, с проверкой каждого найденного места.
  Отдельно: **справочные таблицы и сводки — первое, что расходится** с текстом,
  потому что при точечной правке в них не смотрят.
- **Второй урок, помельче.** Дважды правка ломала соседнее правило тем, что
  переносила термин через границу, где у него сужено значение. В §8.5 слово
  «уничтожение» сужено до назначаемого результата, и добавленный абзац про
  способности пришлось снабжать фразой-переключателем. Проверять надо
  не только наличие правила, но и то, в каком значении в этом месте работают
  его слова.
- **Связанные issue:** —

## D-037 — Пятая категория действий: действия вариантов игры

- **Дата:** 2026-09-11
- **Контекст.** Разбирая двусмысленность «действия вариантов не влезают
  в классификацию §7.2», я отнёс Извлечение сознания и Развёртывание роя
  к уникальным действиям — и **переопределил уже принятое `D-026`**, которое
  велело писать «это основное действие: выполняя его, вы не выполняете в этот
  ход ни общего, ни уникального действия». Проверка поймала противоречие.
- **Решение:** §7.2 получает **пятую категорию — действия вариантов игры**.
  Они не привязаны к фракции (их выполняет тот, кто контролирует нужную
  фабрику), занимают шаг 2 хода и исключают в этот ход общее и уникальное
  действие. Формулировка `D-026` в §11.5 и §11.6 остаётся дословно; в §11.6
  она добавлена — там её не было, хотя `D-026` требовал одинакового текста
  в обоих местах.
- **Почему не «уникальное»:** §7.4 определяет уникальное действие как доступное
  **одной фракции** и печатающееся на планшете. Действия вариантов не отвечают
  ни одному из двух признаков.
- **Урок к `D-036`:** перед правкой классификации или определения проверять,
  не закреплена ли она решением. Сплошного поиска по тексту мало — искать надо
  и по `DECISIONS.md`.
- **Связанные issue:** `RULE-009`

## D-029 — Приёмка фриза P7: три термина, две иконки не дефект, набор иконок делаем в шрифте

- **Дата:** 2026-09-10
- **Контекст:** приёмка фриза структуры, терминологии и иконок. Три термина
  вынесены на решение, две находки icon registry оспорены автором, и задан
  способ реализации недостающих иконок.

### Термины

- **«Ядерная катастрофа» — принято.** «Ядерный катаклизм» выводится из текста
  (`TISS-004`).
- **«Свободная битва» — принято.** «Неограниченная Битва» выводится
  (`TISS-003`).
- **«Перепрошивка» — принято, но условно, и условие проверено.** Alek принимал
  новое слово только если в оригинале у механики тоже было своё имя, а не
  описание. **Проверено по `SRC-RB-NEW`, стр. 173:** «when a Cylinder is
  Captured, you may choose instead to **"Convert"** it into a Cylinder of your
  own Faction's Color». Оригинал берёт слово в кавычки, то есть вводит его
  как термин, и дальше пользуется им уже без кавычек и как глаголом: «if you
  already have four Brain Cylinders in play and Capture a fifth, it cannot be
  **Converted**». Условие выполнено, «Перепрошивка» остаётся (`TISS-006`).
  Попутно из того же абзаца подтверждён предел в 4 энграммы и судьба пятой:
  она кладётся на планшет и в следующую фазу ресурсов возвращается владельцу
  (`BL-UNIT-016`).

### Иконки: две находки P7 сняты автором

- **`ICON-043`, один знак на узле управления и на зонах сети — не дефект.**
  Решение Alek: знак **намеренно** помечает связанные компоненты, которые
  друг без друга не имеют смысла. Правило «один глиф = одно значение»
  не нарушено: значение одно — принадлежность к сети Роя. Заменяется только
  сам emoji, когда появится глиф в шрифте.
- **`ICON-044`, различители десяти особых фабрик — не дефект.** Решение Alek:
  формы различаются, пусть и не идеально, а **основное различие там цвет**.
  Жетоны напечатаны и физически реализованы — **не меняем**.

### Недостающие иконки реализуются в шрифте проекта

- **Решение:** 31 иконка со статусом `MISSING` в `icons.yaml` рисуется
  и добавляется **в кастомный шрифт `Oil Wars`**, а не в виде картинок.
  Причина названа автором: так их можно набирать в любом редакторе, а мне —
  подставлять прямо в текст при сборке PDF и прочих документов, там где
  иконка стоит внутри строки.
- **Это отдельная подзадача**, идёт после правил, вместе с выверкой
  компонентов (`D-004`). Работу над текстом не блокирует: P8 пишется
  плейсхолдерами `{ICON_NAME}`, подстановка глифов — механическая замена
  на финальной сборке.
- **Следствие для сборщика шрифта.** При добавлении глифов чинить два дефекта,
  найденные `CODE-04`: в шрифте лежат три лишние копии `E`/`Q`/`W` под
  строчными кодовыми точками с идентичными контурами (одно изображение
  набирается двумя символами), и `descender` равен −54 вместо −200 из
  исходника FontMaker.
- **Связанные issue:** `TISS-003`, `TISS-004`, `TISS-006`, `ICON-101`

## D-030 — «Очередь» на планшете задаёт и расстановку, и первенство

- **Дата:** 2026-09-10
- **Контекст:** при написании части 5 в P8 я завёл `RULE-007` — пробел в правиле
  о первом игроке. Alek указал, что первенство задаётся фракцией, очерёдность
  остальных расписана и в черновик перенесена, и потребовал выяснить, где она
  потеряна. Сплошная проверка показала: **в тексте правил её не было никогда.**
  - Гуглдок, строка 241, и архивный PDF `Drafts/Нефтяные войны (правила).pdf`,
    строка 296, совпадают дословно: «в начале игры рекомендуется первенство отдать
    игроку выбравшему данную фракцию. В остальных случаях определите первого
    игрока любым удобным вам способом». Ни слова про очерёдность.
  - Приложение по фракциям (`Drafts/Нефтяные войны (фракции).pdf`) в блоке
    «Подготовка» даёт только «8 нефти, 6 рекрутов и одна контролируемая фабрика».
  - **Правило нашлось на компоненте.** Планшет фракции печатает в блоке
    «Подготовка» поле **«Очередь: N»** — рядом с «Запас нефти» и «Регион».
    В шаблоне это `queue` (`Templates/Template.html`, `data-key="queue"`).
    Значения из напечатанных `Templates/Saved/Faction Card — *.pdf`:
    Островная Империя 1, Эйркрафт Корпорейшн 2, Глобал Петролеум 3,
    Клонэйд Ресёрч 7, Североамериканский Альянс 8, Moon Systems 10,
    Братство Сингулярности 10, Сайнтифик Солюшн 10.
  - Значение поля не описано нигде: ни в правилах, ни в приложениях,
    ни в `Code_Wiki.md`, ни в `LLM_CONTEXT.md` — только имя поля.
- **Что говорит оригинал.** Двух вещей, а не одной:
  - первенство — `SRC-RB-NEW` стр. 12 и `SRC-RB-OLD` стр. 421, формулировка
    в обеих вёрстках дословно одна: Great Cthulhu всегда первый, иначе «by any
    means you wish», а рекомендация — по алфавиту названий фракций. Эррата и FAQ
    правило не трогали, проверено сплошным поиском;
  - порядок расстановки — `SRC-RB-NEW` стр. ~68: «The Ancients set up before the
    Tcho-Tcho, Windwalker, and Opener». То есть у фракций без своего стартового
    региона очерёдность расстановки в оригинале задана прямо.
- **Решение Alek:** число «Очередь» задаёт **и то и другое одним числом** —
  и порядок начальной расстановки, и первенство в начале партии. Меньше — раньше.
- **Что из этого пишется в правила:**
  1. 2.4 — фракции расставляются по возрастанию «Очереди»; порядок нужен потому,
     что у части фракций своего стартового региона нет.
  2. 2.5 — первым игроком в начале партии становится фракция с наименьшей
     «Очередью». У Островной Империи это 1, поэтому прежнее правило «если она
     в игре, она всегда первая» сохраняется как следствие, а не как отдельное
     правило. Формулировка «любым удобным способом» из черновика выводится.
  3. **«Очередь» задаёт только, кто первый, а не порядок хода по кругу.**
     Дальше играют от первого игрока в направлении, заданном жетоном
     (`BL-ACTION-001`, `BL-ROUND-003`). Иное означало бы менять механику
     оригинала, где очередь всегда идёт по столу, а не по фракциям.
  4. Таблица номеров в правила не печатается: числа живут на планшетах (`D-021`).
- **Следствия для `DEV-004` и `D-020`.** Решение `D-020` в части «в остальных
  случаях любым удобным способом» перекрыто: способ теперь задан. Часть про
  «всегда» остаётся в силе. Оговорка про Windwalker по-прежнему не переносится.
- **Следствие для `RULE-007`.** Закрыт: в первом раунде фаза определения первого
  игрока ничего не меняет, жетон остаётся у игрока, определённого при подготовке,
  и первый раунд он ведёт безусловно — как в оригинале. Права передать жетон
  у него нет.
- **Равенство «10» у трёх фракций — решение Alek: раздать разные номера.** Это
  правка компонентов (`D-004`), не правил: после неё номера уникальны и правило
  разрешения равенства в тексте не нужно. **До этой правки печатать книгу нельзя** —
  правило 2.5 будет неполным для партий, где нет ни одной фракции с номером 1–8.
- **Связанные issue:** `MISS-018`, `RULE-007`, `DEV-004`

## D-031 — «Очередь» только для расстановки; правила первого игрока как в оригинале

> Уточняет `D-030`. В части «и то и другое одним числом» `D-030` **отменено**:
> проверка оригинала показала, что порядок расстановки и первенство там
> не связаны никак.

- **Дата:** 2026-09-11
- **Контекст:** `D-030` принималось без сплошного разбора оригинала. Alek поставил
  условие: если в оригинале очерёдность существует **только** для расстановки, так
  делаем и мы, а правила первого игрока переносим как есть. Условие проверено
  отдельным проходом по всем десяти файлам корпуса оригинала.
- **Что установлено — факты, не выводы:**
  - **Порядок расстановки в оригинале существует, но он ЧАСТИЧНЫЙ и покрывает
    4 фракции из 12.** Всего два утверждения на весь корпус, оба в разделах
    `Set Up` конкретных фракций: «The Ancients set up before the Tcho-Tcho,
    Windwalker, and Opener» (`SRC-RB-NEW` стр. 68) и «once all other players except
    for Windwalker and Opener have set up, the Tcho-Tcho set up in any unoccupied
    Area with a Faction Glyph» (`SRC-RB-NEW` стр. 89). Windwalker и Opener своих
    правил порядка не имеют — их позиция задана этими двумя.
  - **Полного линейного порядка из оригинала собрать нельзя.** Семь остальных
    фракций между собой не упорядочены, и Ancients относительно них тоже.
    Подтверждено сплошным поиском: `set up after`, `set up last`, `set up first`,
    `setup order`, `order of setup`, `last to set up`, `first to set up`,
    `who sets up`, `before any other` — **0 вхождений** по всем файлам.
  - **Оригинал фракции нигде не нумерует.** `Faction 1`, `Faction #`,
    `Faction Number`, `1. Great Cthulhu` — 0 вхождений. Нумерации нет ни
    в оглавлении, ни в перечне компонентов, ни в таблицах эрраты.
  - **Расстановка и первенство независимы полностью.** Ни одно правило порядка
    расстановки не упоминает первого игрока, направление хода или порядок игроков;
    правило первого игрока не упоминает расстановку. Соседство подразделов
    на одном развороте — только вёрстка, перекрёстных ссылок нет.
  - **Правило первого игрока не менялось ни эрратой, ни FAQ.** `SRC-ERRATA-PACK`
    (12.2023) содержит 4 вхождения `First Player`, все — пересказ игровой фазы
    в карте-памятке. В `SRC-FAQ` три вхождения, ни одного про старт партии.
- **Решение:**
  1. **«Очередь» на планшете задаёт только порядок начальной расстановки.**
     На первенство она не влияет, и это сказано в 2.5 прямым текстом, чтобы
     за столом не связали два правила по соседству.
  2. **Правила первого игрока переносятся как в оригинале.** Островная Империя
     в партии — её игрок первый всегда; её нет — любым удобным способом,
     рекомендация по алфавиту названий фракций. Оговорка про Windwalker
     не переносится, аналога нет (`D-020`).
  3. **«Очередь» печатается у всех фракций, а не только у тех, что без своего
     стартового региона.** Это наше отступление по форме, не по смыслу:
     оригинал выражает то же правило россыпью по разделам фракций, у нас оно
     собрано в одно поле планшета. Причина названа Alek: раздал планшеты, глянул
     число — и порядок понятен без чтения правил.
  4. **Текст 2.4 не утверждает общего правила «сначала с логотипом, потом без».**
     Такого правила в оригинале нет: Ancients своего логотипа не имеет, но
     обязан идти раньше трёх названных фракций. 2.4 называет механизм (число
     на планшете) и причину (фракции без своего региона должны видеть занятое),
     а точный порядок несут сами числа.
- **Распределение чисел — задача выверки компонентов (`D-004`), и она
  не выполнима без карты соответствия фракций.** Что для неё установлено:
  - шкала берётся на полный набор оригинала — 12 позиций, наши восемь занимают
    свои места, свободные остаются под недоделанные фракции;
  - порядок внутри шкалы обязан воспроизводить два правила оригинала выше,
    а не выдумывать ранги там, где оригинал молчит;
  - **нужна карта соответствия наших восьми фракций оригинальным.** Сейчас
    подтверждены две: Островная Империя = Great Cthulhu (`D-020`),
    Moon Systems = Bubastis (`D-011`). Остальные шесть не выверены — это часть
    задачи по фракциям.
  - Пока номера не распределены, печатать книгу нельзя: 2.4 ссылается на числа,
    а три фракции носят одинаковую «Очередь: 10».
- **Связанные issue:** `MISS-018`, `SRC-003`, `RULE-007`

## D-032 — Производство боевой машины и меха сливается в одно действие

- **Дата:** 2026-09-11
- **Контекст:** `DEV-021`. В оригинале у мехов своё действие — Summon Terror.
  Alek: если слияние возможно без поломки и без списка исключений — сливаем;
  если сломает или потребует кучи оговорок — делим. Проведён сплошной разбор.
- **Что установлено:**
  - **Официальная эррата уже провела это слияние сама.** `SRC-ERRATA-PACK`, стр. 12:
    «Terrors are summoned **as though they are Monsters**, and require a Controlled
    Gate to be able to enter play» — ни слова «Action», ни имени «Summon Terror».
    Рулбук эпохи Final Onslaught писал «with a Summon Terror Action»; эррата
    от 12.2023 свежее и по `D-015` побеждает.
  - **Summon Terror не был общим действием даже в рулбуке.** «seven Common Actions» —
    7 вхождений, «eight Common Actions» — 0. В перечне семи общих действий его нет,
    раздел «Terrors» лежит в главе дополнений, а не в главе фазы действий.
    Официальная карта-памятка перечисляет ровно семь действий, строки Summon Terror
    в ней нет.
  - **Имя действия во всём корпусе упоминается только внутри определения самого
    действия** — 5 вхождений, все перечислены в отчёте. Ни один spellbook,
    ни одна карта лояльности, ни одна фракционная способность, ни одно правило
    не ссылается на «Summon Monster Action» по имени. Нулевые шаблоны:
    `as a Summon Monster`, `counts as a Summon Monster`, `additional Summon`,
    `extra Summon`, `second Summon`, `another Summon Monster`,
    `instead of Summoning`, `free Summon`, `additional Action` — **по нулю**.
  - **Все способности, трогающие призыв, адресуют ТИП ОТРЯДА или глагол, но
    никогда действие.** Brainless, Festival, Extinction — через конкретный тип
    юнита. Fertility Cult (Black Goat, «may Summon any number of Monsters at once»)
    — через тип. Slime Mold («Slimes are Monsters, but they cannot be Summoned»)
    — через тип. Значит слияние действий ни одну формулировку не задевает.
  - **Единственный пробел, который слияние обязывает закрыть:** лимит «1 за действие»
    оригинал задаёт только для Monster и про Terror молчит (`only Summon 1 Terror`,
    `1 Terror`, `one Terror`, `single Terror`, `Terror per` — 0 вхождений).
- **Решение: сливаем.** Действие называется **«Производство боевой машины или
  меха»**, и это отступление по форме, разрешённое Alek. Требуется ровно одна
  формулировка, списка исключений не возникает:
  **«За это действие производится ровно один отряд — боевая машина или мех.»**
  Это воспроизводит поведение боевой машины 1:1 и закрывает молчание оригинала
  по меху самым строгим чтением.
- **Что при слиянии сохраняется отдельно и в тексте проговорено:** мех остаётся
  отдельным типом отряда, и способности, нацеленные именно на боевые машины,
  на него не действуют (3.5, `BL-UNIT-005`). Раздельность в оригинале объявлена
  **только** для «abilities that specifically target Monsters» — то есть она про
  юниты, а не про действие, и слияние действий её не касается.
- **Правило письма на будущее, следствие разбора:** любая наша способность,
  меняющая производство — стоимость, количество, запрет, — должна называть
  **тип отряда**, а не действие. Оригинал делает так везде без исключений; пока
  мы держим то же, слияние остаётся безопасным. Проверить при выверке технологий
  и карт лояльности (`D-004`).
- **Связанные issue:** `DEV-021`

## D-033 — Стартовый запас: правила остаются, планшет чинится — ОТМЕНЕНО

> ⚠️ **Отменено решением `D-035` (2026-09-11).** Не выполнять. P9 показал, что
> перестройка стартового запаса не эквивалентна оригиналу, `DEV-002` открыт
> заново, и правила приведены к оригиналу: 8 нефти выдаются при подготовке.
> Планшеты после этого совпадают с правилами и **не правятся**. Ниже —
> исходный текст решения, оставлен для истории.

- **Дата:** 2026-09-11
- **Контекст:** `SRC-004`. Планшеты печатают «Запас нефти: 8», правила начинают
  партию с нуля и выдают те же 8 первой фазой получения ресурсов. По букве двух
  источников игрок входит в первую фазу действий с 16 вместо 8.
- **Решение Alek:** правила остаются как есть — способ другой, результат тот же,
  и это уже разбиралось при закрытии `DEV-002`. Строка «Запас нефти: 8» убирается
  **с планшета**, позже, в выверке компонентов.
- **Следствия:** текст 2.4 и 2.6 не меняется. `SRC-004` переходит в `DECIDED`
  и остаётся блокером печати планшетов, а не книги. Правка — поле `oil`
  в восьми файлах `Templates/Saved/*.json` плюс пересборка PDF; то же в приложении
  по фракциям, если оно останется в работе.
- **Связанные issue:** `SRC-004`, `DEV-002`

## D-034 — Ссылки из глоссария ведут в правила; четыре термина переименованы

- **Дата:** 2026-09-11
- **Контекст:** приёмка части 13. Три вопроса, поднятых при сверке глоссария.
- **1. Ссылки из глоссария ведут ТОЛЬКО в правила, а не в другие статьи
  глоссария.** Причина названа Alek: глоссарий строится по алфавиту, а не по
  связности тем, и в этом его смысл. Рабочий путь читателя — встретил термин,
  забыл значение, открыл глоссарий, прочёл краткое напоминание; не хватило —
  прошёл по ссылке в правила и перечитал нюансы. Ссылка на соседнюю статью этот
  путь обрывает.
  Следствия: все «См. **статья**» из глоссария выведены; каждая статья
  заканчивается номером раздела правил; требования `STRUCTURE.md` §13 пункты 3
  и 4 выполняются **противопоставлением внутри статьи**, а не взаимной ссылкой.
- **2. Требование 2 §13 переформулировано.** «Ход» и «Раунд» не могут «стоять
  рядом» при алфавитном порядке: это позиции 29 и 45. Требование оставлено
  в части взаимного определения, невыполнимая часть снята.
- **3. Четыре записи `terms.yaml` приведены к написаниям, которые стоят в книге.**
  Карта терминов была заморожена в P7, и при фризе разошлась со `STRUCTURE.md`
  §13 в четырёх местах. Alek разрешил правку; поле `redesign` изменено:
  - `TERM-053` «Незанятая военная фабрика» → **«Незанятая фабрика»**;
  - `TERM-031` «Тактический (боевой) эффект» → **«Тактический эффект»**;
  - `TERM-032` «Финальный (боевой) эффект» → **«Финальный эффект»**;
  - `TERM-033` «Постоянно» → **«Постоянный эффект»**.
  Причина: это заголовки статей и слова в тексте правил. «Постоянно» заголовком
  не работает вовсе, скобка «(боевой)» в заголовке лишняя. Игровое поведение
  не меняется ни в одном из четырёх случаев.
- **4. Расстановка фракций: правила только отсылают к номерам на планшетах.**
  Пояснение о том, зачем нужен порядок, из 2.4 убрано как лишнее. Сами номера
  сверяются и расставляются отдельной задачей (`FAC-008`).
- **Связанные issue:** `TISS-011`, `FAC-008`

---

## D-038 — Раздел F diff P9: четыре добавки оказались правилами оригинала, одна была расхождением

**Дата:** 2026-09-15. **Контекст:** раздел F отчёта `out/09-mechanical-diff.md`
перечислял шесть формулировок книги, под которые не нашлось записи эталона.
Проверка тремя независимыми субагентами по полному корпусу источников показала,
что «добавками» они были не все.

**Главный вывод: отсутствие записи в `baseline.yaml` — не доказательство
добавки.** Четыре из шести формулировок в оригинале есть, просто лежат там, куда
эталон не заходил: во врезке Note раздела Independent Great Old Ones, в карточке
модуля Yuggoth, в ответе FAQ. Эталон собирался по ядру правил, и это его граница,
а не граница оригинала. Дальше «нет в эталоне» проверяется сплошным поиском
по источникам, и только потом объявляется нашим.

### F1. Совет про число наёмных роботов — **правило оригинала**

SRC-RB-NEW, с. 109, врезка Note: «For your first game with Independents, we
recommend using one fewer Independent than the number of players». У нас стояло
императивом «возьмите». Приведено к оригиналу: это **рекомендация**, не правило.
Добавлена вторая рекомендация оттуда же (с. 107) — не вводить наёмных роботов,
пока не сыграно несколько партий. Совет отделён от абзаца про фракционного
робота в роли наёмного: в оригинале это разные места (с. 109 и с. 112), склейка
делала из совета продолжение чужой мысли.

### F2. Место появления наёмного робота — **было расхождение, исправлено**

Мы писали: фигурка ставится «в любой регион с контролируемой вами фабрикой, если
на карте не сказано иное». В оригинале дефолта нет вовсе: «take its Loyalty Card
and place its figure on the map» — и всё, место берётся с карты. А типовая карта
(SRC-ERRATA-PACK, тексты карт лояльности) требует «Your Controlled Gate is in an
Area with your Great Old One… place <робота> in the Area containing the Gate».

**Наша формулировка была шире оригинала** и разрешала поставить робота на фабрику,
в регионе которой нет вашего боевого робота, то есть туда, где условие создания
не выполнено. Переписано: условия и место читаются с карты, типовая карта описана
как типовая, дефолта нет.

Попутно из FAQ: юнит, который сам считается фабрикой (в оригинале Yog-Sothoth),
условию «контролируемая фабрика» **не** удовлетворяет. **Такой юнит у нас уже
есть** — «Шрёдингер» Сайнтифик Солюшн, мобильная военная фабрика
(`factions.yaml`, `MAP-07`, CONFIRMED/HIGH). Значит случай не гипотетический:
«Шрёдингер» не засчитывается как контролируемая фабрика в условии создания
наёмного робота. Заведено отдельным issue на выверку составов (`D-004`).

### F3. Ничейный Рой — **правило оригинала**, формулировка расширена

SRC-RB-NEW, с. 177: «If an uncontrolled Slime Mold is Pained (or Retreated)…
their attacker chooses the Area to which they are Pained». Мы писали только про
подавление — расширено до любого отступления, как в оригинале.

**Оговорку оригинала «and Madness is not in effect» я сначала опустил, и это была
ошибка.** Обоснование было «исключение под технологию фракции Crawling Chaos,
которой в нашем составе нет». Crawling Chaos в составе **есть**: это Эйркрафт
Корпорейшн (`factions.yaml`, `MAP-02`, CONFIRMED/HIGH), её технология «Сбой
коммуникации» опознана как Madness. Безусловное «решает атакующий» перебивало бы
эту технологию.

Исправлено в тот же день: §11.6 говорит, что регион выбирает **тот, кто вызвал
отступление** (в битве атакующий, вне битвы — игрок, применивший способность),
и отдельным абзацем вводит исключение — если регион отступления назначает
способность (8.7), ничейным Роем распоряжается она.

**Урок к `D-036`:** «такого у нас нет» — утверждение о составе фракций, и
проверяется оно по `registry/factions.yaml`, а не по памяти. Карта соответствия
восьми фракций оригинальным собрана и лежит там.

### F4. Невозврат нефти за объявление битвы — **правило оригинала**

FAQ, Core Game Rules: «The Power is paid to initiate the Battle, but no Battle
actually takes place!» Оставлено без изменений. Наша формулировка шире тела
рулбука («до бросков кубиков» против «during Pre-Battle»), и это правильно:
FAQ распространяет правило ровно на тот более широкий случай.

### F5. Окончание игры вне фазы влияния — **наполовину наше**

Часть про фазу действий подтверждена дословно (SRC-RB-NEW, с. 42). Для фазы
получения ресурсов и фазы определения первого игрока оригинал момент окончания
**не задаёт**, хотя раскрывать скрытое влияние в них прямо разрешает («at any
time: during any of the four Phases of play»). Правило по аналогии с фазой
влияния — единственное непротиворечивое, поэтому оставлено как наше достроение.
Формулировка «в любой другой фазе» заменена на прямое перечисление двух фаз,
чтобы читатель не искал непокрытую четвёртую.

### F6. Очередь расстановки — **наше, но уже решено**

Числа «Очередь» на планшете в оригинале нет; сквозного порядка расстановки для
всех фракций там тоже нет — порядок задан россыпью в разделах отдельных фракций
(«The Ancients set up before the Tcho-Tcho…»). Это `D-030`/`D-031`, ничего
не меняем. Открытыми остаются два хвоста по компонентам, оба заведены:
`MISS-018` (числа не распределены, три фракции носят «Очередь: 10») и `SRC-003`
(у Сайнтифик Солюшн 10 в PDF против 8 в JSON).

**Связанные issue:** `EXTRA-002`…`EXTRA-005`

---

## D-039 — Белая фабрика опознаётся знаком ✦; вариант «Особые фабрики» играбелен

- **Дата:** 2026-09-11 (устно, в журнал занесено 2026-09-15)
- **Контекст:** `RULE-010`. Правило «создать белую фабрику можно, только если вы
  уже контролируете любую другую особую» механически верно, но я не смог найти,
  какая из десяти особых фабрик «белая»: `Factory.html` различает их десятью
  юникодными знаками и цветом, а какой из них белый — нигде не записано. Был
  вывод «вариант неиграбелен полностью».
- **Решение Alek:** находка снята. Белая фабрика — та, что помечена знаком **✦**;
  на жетоне она различается глазом прекрасно, жетон читается хорошо.
  Опознаваемость обеспечена цветом и знаком, как у остальных девяти
  (`ICON-044`, `D-029`).
- **Следствия:** текст 11.1 остаётся с написанием черновика — «белая фабрика».
  Термин в `terms.yaml` не заводится: слово указывает на цвет жетона, а не
  на отдельную механическую сущность. Вариант «Особые фабрики» играбелен,
  блокера нет.
- **Почему решение занесено задним числом:** оно было принято устно и не попало
  в журнал, из-за чего `RULE-010` остался без `decision_ref` и валидатор реестра
  падал. **Устное решение — это решение; в журнал оно должно попадать в тот же
  ход.**
- **Связанные issue:** `RULE-010`, `ICON-044`

---

## D-040 — Раздел E diff P9: целевой состав 3–5, компоненты под 6–8 не делаем

- **Дата:** 2026-09-15
- **Контекст:** девять вопросов раздела E отчёта `out/09-mechanical-diff.md` —
  состав компонентов против оригинала.
- **Сквозной принцип, заданный Alek:** **целевой состав — 3–5 игроков. Правила
  партии на больший состав из книги не выбрасываются, но дополнительные
  компоненты под него не реализуются.** Этим закрываются пункты 2, 3, 4, 5, 6
  и 7 разом: всё, что «не сходится при восьми игроках», не сходится вне нашего
  целевого состава.

### Решения по пунктам

**1. «Баланс сил» оставляем как есть.** Правило маловажное, а при целевом
составе до пяти игроков неучаствующие фракции всегда есть. Дополнительно Alek
предложил второй носитель: **вместо фигурки чужой фракции можно взять
фигурку энграммы** — на старте партии энграмм нет ни у кого, и формой они
отличаются от рекрутов. Набора из девяти нейтральных серых рекрутов, как
в оригинале, мы не заводим.

**Важная оговорка, без которой правило ломалось.** Первая редакция говорила
просто «или энграмма», и это столкнулось с тремя местами книги: энграмму нельзя
завербовать, она выводится на поле только Извлечением сознания, её предел —
четыре на фракцию, и ей нельзя назначить подавление. Слабый игрок, получивший
«седьмого рекрута», не смог бы его завербовать вовсе.

§11.7 переписан так, что конфликта нет: берётся **фигурка** энграммы, а не
энграмма как отряд. Фишка-заменитель считается обычным рекрутом, правилами 11.5
не пользуется и в предел четырёх не идёт. Для партии с Лабораторией добавлено:
берите фигурку энграммы фракции, которая в партии не участвует, — тогда ничьи
четыре не пострадают. `D-023` остаётся в силе: механическая эквивалентность
оригиналу сохранена, добавился только второй носитель.

**2. Диапазон снят с титула.** Строка «Игра для 3–5 участников» убрана, осталось
«Правила». Вместо неё — абзац в **§1.4**, там, где описано устройство поля: игра
рассчитана на 3–5, партию можно сыграть и на 6–8, но компоненты под такой состав
в комплект не входят. Та же оговорка под таблицей раскладок в §2.1, со ссылкой
на 1.4.

**3. Памяток остаётся 5.** По целевому составу.

**4. Боевая сила Роя остаётся на карте Контроля сети.** В книге не дублируется
(`D-021`). Карту ещё предстоит сделать — **число 2 обязано на ней появиться**,
это `MISS-014`.

**5. Треков Судного дня два, оба двусторонние.** §1.1 поправлен.

**6. Списки регионов Лаборатории для 6–8 не дописываются.** В оригинале их нет
ни для одной раскладки, наши списки на 3/4/5 — наша разработка.

**7. Прочерки в таблице регионов для 6/7/8 остаются.** Оригинал этих чисел тоже
не даёт. Под таблицей сказано, что поле на 6–8 в комплект не входит.

**8. Игровое поле — решение отложено.** Сколько частей и какие стороны, будет
ясно после реализации компонента. В книге сейчас стоит формулировка по
оригиналу: в партии всегда собираются две части. **Проверить и при
необходимости поправить, когда поле будет сделано** — `MISS-023`.

**9. Кубики: числовые по умолчанию, символьные по желанию.** В §8.4 добавлен
абзац: лист наклеек превращает обычные кубики в боевые — смерть на одной грани,
подавление на двух, три грани пустые (промах). Распределение то же.
В §1.1 кубики описаны как «20 шестигранных плюс лист наклеек — по желанию».

**Что при этом обнаружилось в компоненте.** Лист наклеек собран, и он устроен
не так, как в оригинале. Генератор `Templates/generate_dices.py` задаёт раскладку
прямо: **20 знаков смерти, 40 знаков подавления и по 20 цифр 1, 2 и 3** — то есть
шесть наклеек на кубик, и три грани промаха несут **цифры**, а не остаются
пустыми. На чёрном кубике пустая грань и невозможна: цифру там надо чем-то
закрыть. Оригинал делает иначе — у Battle Dice грань промаха пустая, — и первая
редакция §8.4 описывала оригинал, а не наш компонент. Исправлено: «цифры 1, 2
и 3 на остальных трёх гранях — они и означают промах».

`Templates/Dices.html` при этом содержит только один блок из тридцати цифр «3»:
это остаток ручной допечатки, а не текущее состояние листа. Готовый лист —
`Saved/Dices Print Sheet.pdf`.

Арт двух символов: `Templates/dices/kill.png` и `retreat.png` (плюс варианты
`-simple`). Иконки книги `{KILL}` и `{SUPPRESS}` обязаны взять **этот** арт,
а не новый рисунок. Имя файла `retreat` осталось от терминологии до `D-019` —
переименовать в подавление при работе над компонентами. Заведено `MISS-024`.

- **Связанные issue:** `MISS-014`, `MISS-023`, `MISS-024`

---

## D-041 — Батч P10: семь находок симулированной партии

**Дата:** 2026-09-16. **Контекст:** Alek разобрал находки P10 и по семи из них
дал решения; три требовали предварительной сверки — по оригиналу или по нашему
компоненту.

### PLAY-002 — символы на поле: правки не будет (WONTFIX)

Alek: значения символов расписаны у Глобал Петролеум на планшете и карточках,
для остальных фракций они значения не имеют, в правилах не требуются.
Формально это разметка общего компонента, но читает её только та фракция, чьи
задачи на неё завязаны, — и читает со своих же карт.

### PLAY-006 — числовой кубик: заводим один обычный d6

Alek: «заведём один обычный не кастомный d6, можно отдать его в компоненты
фракции». В §1.1 добавлен компонент, в §8.4 — абзац «Числовой кубик». Закрыты
оба дефекта: бросок вне битвы описан, и снята ловушка «оклеил все двадцать —
способности на число стали невыполнимы».

### PLAY-011 — влияние выше 30: трек длиннее порога

Alek: «влияние выше 30 есть куда отмечать, у нас трек до 44 сейчас». §3.8 больше
не утверждает «от 0 до 30»: тридцать — порог окончания партии, а не потолок
счёта. **Число 44 в книгу не вписано намеренно:** трек влияния как компонент
в проекте не найден, и пока разметка не закреплена, число в тексте — обязательство,
которое придётся переписывать. Решить, называть ли предел числом, когда компонент
будет собран.

### PLAY-003 — несколько результатов на один отряд: разобрано по оригиналу

**По умолчанию один отряд получает один результат.** Прямого запрета в оригинале
нет, но правило однозначно следует из механики избытка: «If the number of Kills
rolled exceeds the number of **vulnerable** Units, ignore the excess results»
(SRC-RB-NEW, с. 29). Понятие «лишних» смертей возможно только при соотношении
один результат : один отряд. Подтверждено разбором Complex Battle Example 7,
где учёт ведётся по отрядам «without a result».

**Отряд, которому назначена смерть, перестаёт быть целью.** FAQ: «If you assign
the Kill to the other Unit, then the Pain is ignored». Трюка «утопить лишние
подавления в отряде, который и так убит» в оригинале нет — и именно ради этого
существует жёсткий порядок «сначала смерти, потом подавления».

**Исключение — способность, дающая пережить смерть.** Такой отряд остаётся целью
и может получить ещё и подавление; тогда он отступает, а не гибнет. Это ровно
наша «Комбинированная броня» (оригинал — Regenerate у Great Cthulhu), и текст
нашей карты совпадает с поведением из Complex Battle Example 5 дословно.

Три абзаца внесены в §8.5 перед «Лишние результаты игнорируются» — раньше тот
абзац висел без опоры: было непонятно, откуда вообще берётся избыток.

### PLAY-004 — «выведен из битвы, оставшись в регионе»: это не новое состояние

Ключевая находка: **в оригинале это не отдельное состояние, а часть одного общего
понятия.** SRC-RB-NEW, с. 28: «Some Pre-Battle abilities Eliminate, Kill,
**or otherwise remove a Unit from the Battle**… that Unit does not participate
for the remainder of the Battle… Any Battle-related ability possessed by that
Unit cannot be used». Примером к этому правилу там же стоит именно Invisibility —
наш «Стелс».

Значит §8.3 ничего не терял, а **сузил**: прочитал «удалил» как «убрал с поля».
Правка — возврат к 1:1.

Ответы оригинала на вопросы, вставшие в партии: битва окончена, если у стороны
не осталось участвующих отрядов; кубиков не бросает ни одна сторона; нефть
не возвращается; финальные эффекты выведенного отряда не применяются
(«This includes all Post-Battle abilities and effects»); отряд остаётся в регионе
и вне битвы действует — FAQ разрешает выведенному Стелсом отряду реагировать
своей способностью.

По компонентам ответ на прямой вопрос Alek «а на планшете и карточках?»:
состояние создаёт **ровно один** наш компонент — карта технологии «Стелс», и
только она же его упоминает; на планшете Эйркрафт Корпорейшн её нет, технологии
живут отдельными картами. Механику состояния не объясняет ни один компонент.

§8.3 и §8.9 переписаны через «выведен из битвы» с явным разведением двух
способов: убрать с поля или оставить в регионе, исключив из битвы.

### PLAY-001 — поле сверено по компоненту, книга верна

Находка снята. Состав обеих сторон снят с карты глазами:

- **сторона «3»**, 13 регионов: семь сухопутных (Северная Америка, Южная
  Америка, Европа, Азия, Африка, Австралия, Антарктида) и шесть океанских;
- **сторона «5»**, 21 регион: пятнадцать сухопутных и **те же шесть** океанских.

Отсюда: §3.3 «океанских регионов шесть» верно на обеих сторонах — океаны
при переходе на сторону «5» не дробятся, дробится только суша. Числа 13 и 21
из §2.1 сходятся точно.

Ошибка была в посылке проверяющего: он считал, что на стороне «3» есть
Аравийский полуостров и Южная Азия. Их там нет.

### PLAY-013 — поимённые списки регионов убраны из книги

**На стороне «3» Аравийского полуострова не существует**, а §11.5 предлагал его
для партии на троих. Ошибка унаследована из черновика (SRC-DRAFT, строка 643):
списки писались без сверки с картой.

Первая правка чинила списки поимённо — и попалась на второй половине того же
правила. **Все три списка рекомендовали стартовые регионы:** Азию на троих (старт
Эйркрафт Корпорейшн), Европу и Западную Африку на четверых и пятерых. То есть
книга советовала ровно то, что сама запрещает пунктом 1 того же раздела. Я сверил,
существует ли регион на раскладке, и не сверил, не занят ли он стартом.

**Итог: поимённые списки из §11.5 убраны целиком**, вместо них качественные
соображения — Лаборатория в середине карты переходит из рук в руки, на отшибе
стоит спокойнее; при мобильных фракциях берите дальше от центра; чем меньше
игроков, тем меньше выбор. Единственное связывающее правило — пункт 1. То же
в §11.6: поимённые примеры Узла и Зон сети заменены описанием двух схем.

Причина именно такого решения, а не починки списков: **по карте идёт отдельный
аудит**, который соберёт состав регионов и стартовые позиции по всем раскладкам.
Пока его данных нет, поимённый список в книге — обязательство, которое нечем
проверить. Вернуть списки можно будет по его итогам, если они окажутся полезны.
`PLAY-013` остаётся `DECIDED`: правка внесена, вопрос закрывается вместе
с аудитом.

### SRC-001 переоткрыт: у нас всё-таки есть аналог невидимости

Правка §8.3 едва не переопределила уже принятое решение, и это стоит записать
отдельно. `SRC-001` (решено `D-015`) разбирает расхождение источников: считается
ли отряд, выведенный из битвы, присутствующим в регионе — «for this purpose»
или «for any purpose». Принята версия рулбука, «для любых целей», **с оговоркой
«на нас, скорее всего, не влияет: аналога невидимости у наших фракций нет»**.

Аналог есть. Это «Стелс» Эйркрафт Корпорейшн = Invisibility. Значит выбор
формулировки перестал быть теоретическим: от него зависит, держит ли выведенный
отряд фабрику во время битвы, блокирует ли отступление противника в свой регион
(8.7) и прикрывает ли пехотинца при захвате (7.3.7).

Первая редакция §8.3 отвечала на это сама — «он там стоит, держит фабрику», —
то есть молча переворачивала решение. Формулировка снята. Сейчас §8.3 говорит
ровно столько, сколько бесспорно: отряд с поля не убирается и в следующей битве
участвует как обычно. **`SRC-001` переведён в `AUTHOR-DECISION-REQUIRED`:**
подтвердить «для любых целей» или пересмотреть, и тогда в §8.3 добавится одна
фраза. Обратная улика в пользу подтверждения: наша карта «Стелс» написана
как «как если бы их не было в регионе».

Третий раз за проект ловится одна и та же ошибка: **правка, отвечающая
на вопрос, по которому есть решение.** `D-037`, раздел B2 отчёта P9, теперь это.

### Что вынесено на отдельное решение

- **Устройство поля.** В `Карта/` лежит по одному цельному файлу на сторону
  (`карта.pdf` — «3», `карта 5.jpg` — «5»), а не две половины; старые файлы
  с «print» в имени — тот же цельный лист под домашний пробник. Книга же говорит
  про две части и про смешанную раскладку на четверых, где одна половина лежит
  стороной «3», другая стороной «5». Собрать такую раскладку из того, что есть,
  нельзя. Дописано в `MISS-023`.
- **Разнобой формулировок «Стелса»:** на карте «не участвуют в дальнейшей битве
  (как если бы их не было в регионе)», в приложении по фракциям — «**никак**
  не участвуют», без скобки. Заведено `MISS-025`, решать вместе с `SRC-001`.
- **Расхождение источников по выведенному отряду** — это `SRC-001`, см. выше.

**Связанные issue:** `PLAY-001`…`PLAY-004`, `PLAY-006`, `PLAY-011`, `PLAY-013`,
`MISS-023`, `MISS-025`, `SRC-001`

---

## D-042 — Батч P10/P10B: тридцать находок двух симулированных партий

**Дата:** 2026-09-16. **Контекст:** шестнадцать открытых находок первой партии
(`PLAY-005`…`PLAY-023`) и четырнадцать второй (`PLAY-024`…`PLAY-037`). Alek дал
решения по семи блокирующим, четыре из них потребовали сверки с оригиналом.

**Итог: 27 применены, 2 ушли в выверку компонентов, 1 возвращается автору.**

### Решения Alek

**`PLAY-024` — логотипов на поле нет.** «Стартовый регион фракции указывается
на её планшете». §2.4 переписан: регион назван на планшете строкой «Регион»,
на поле он ничем не отмечен. Следом поправлены статья глоссария «Логотип
фракции» (логотип живёт на планшете, маркере и фигурках), статья «Стартовый
регион» и блок Д описания планшета в §1.3, где логотип назывался тем, «которым
отмечен ваш стартовый регион на карте».

**`PLAY-029` — раскладок на четверых две, играть можно на любой.** «Даёт
разнообразие». Внесено в §2.1: это разные партии, регионов в обеих семнадцать,
но регионы другие и стартовые места смещаются; по умолчанию стороной «5»
кладётся половина с Европой, Азией и Африкой.

**`PLAY-037` — предположение Alek подтвердилось дословно.** «Такой отряд просто
выбыл из битвы и никак дальше не учитывается». FAQ отвечает ровно на наш случай:
«Or if Sleeper simply Moves all of his units out of the Area? — …The Battle ends,
no dice are rolled, and no abilities (including Pre-Battle!) can be used.
The Power is paid to initiate the Battle, but no Battle actually takes place!»
Причина в оригинале намеренно не перечисляется — «if all Units of one Faction are
**somehow** removed». Наши §8.3 и §8.9 перечисляли два способа и тем сужали
правило. §8.9 переписан через состояние.

### Что показала сверка с оригиналом

**`PLAY-033` — наша книга инвертировала правило.** Юнит, который сам считается
фабрикой, в оригинале **всегда** считается фабрикой под контролем владельца:
«he is never considered to be Abandoned», «Opener gets 2 Power for Yog-Sothoth
during the Gather Power Phase. He also gets 1 Doom…». По букве нашей книги было
наоборот: пехотинца на нём не бывает, значит он всегда незанятая фабрика — и
кормил по 1 нефти каждого противника, не давая владельцу ничего. Внесено:
третий случай в §3.6, оговорки в §4.1 и §6.2, правка глоссария. Термин
не вводится, `terms.yaml` заморожен — признак описан по месту.

Попутно подтверждено, что §4.1 «1 нефть за незанятую фабрику **каждому**
игроку» — точный перенос («each player gains 1 Power per Abandoned Gate»).

**`PLAY-034` и `PLAY-005` — одна ошибка, одна правка.** §8.4 выносила формульную
боевую силу за пределы суммы. В оригинале формула — обычный Combat rating:
«some require a simple calculation… your Faction's Combat is equal to the sum
of the Combat rating of all of your Units involved in the Battle». И формула
вправе считать то, что вне региона битвы, — FAQ по Chaugnar Faugn считает
вражеских боевых роботов на всей карте и складывает результат с остальной силой.
§8.4 переписан, §8.1 прямо говорит, что формульная сила идёт в сумму.

**Компонентное следствие, важное:** на планшете Североамериканского Альянса
«Абрамс 2220» имеет силу 0, а броски вынесены в отдельный блок «Разведывательная
информация». По оригиналу это и есть его боевая сила, заданная формулой. Три
несостоявшиеся битвы за партию — прямое следствие этого расхождения. Правка
планшета: проставить формулу вместо нуля, отдельный блок убрать.

**`PLAY-026` — «кроме стартовых регионов других фракций» считается по всем
фракциям игры,** а не только по играющим. Прямой фразы в оригинале нет, вывод
опирается на два места: правило Tcho-Tcho работает только если глифы
отсутствующих фракций считаются, а FAQ по Primeval отдельно оговаривает
противоположный случай — значит по умолчанию считаются. Чтобы правило было
исполнимо без глифов на поле, §2.4 говорит, где взять данные: стартовые регионы
напечатаны на планшетах, при неполном составе смотрят планшеты неучаствующих
фракций.

Попутно снята ошибка постановки задачи P10B: «Сайнтифик Солюшн стартует
с меньшим запасом и без отрядов на карте» — это Daemon Sultan, фракция вне
нашего состава. Наш планшет совпадает с оригинальным Opener of the Way.

**`PLAY-032` — «одна фабрика в регионе за всю партию» снято.** Оригинал говорит
о состоянии, а не об истории региона, и сам двигает фабрики способностями.
Формулировка: «больше одной фабрики одновременно».

### Остальные двадцать

Внесены без отдельных решений, каждая — по своему предложению:

- **подготовка:** пул фракций для первой партии вместо раскладки (`PLAY-015`);
  перечёркнутые деления трека пропускаются (`PLAY-028`); «шесть рекрутов» вместо
  «шести пехотинцев» (`PLAY-021`); правило узкого и широкого региона из `MF-009`
  перенесено в книгу (`PLAY-027`);
- **ресурсы:** порядок собственных эффектов в фазе (`PLAY-008`); запас нефти —
  открытая информация (`PLAY-007`);
- **влияние:** шаг 2 пропускается при Ядерной катастрофе (`PLAY-016`);
- **действия:** окончание фазы действий через полный круг (`PLAY-010`); момент
  паса (`PLAY-022`); лимит задачи стоит на самой задаче (`PLAY-009`); нефть
  платится за отряд, а не за регион (`PLAY-018`); вторым действием годится любая
  категория (`PLAY-019`); способность, ставящая отряд, действием не является —
  оговорка поднята из §7.3.2 в §3.9 (`PLAY-017`);
- **битва:** сторона с силой 0 не бросает (`PLAY-031`); собственно боевые эффекты
  названы «Битва. Сражение» (`PLAY-030`);
- **общее:** бонус на карте задачи (`PLAY-020`); текст способности побеждает
  общее правило книги (`PLAY-036`); фабрика становится незанятой в момент ухода
  пехотинца, чем бы он ни был убран (`PLAY-014`); исполнитель назван — первый
  игрок тянет карту события и выбирает регионы вариантов, Зон сети четыре
  (`PLAY-012`).

### Что не закрыто

1. **`PLAY-025` — символы Q/W/E, возвращается Alek.** Подробности — в самой
   записи. Коротко: в оригинале это три Spellbook Glyph фракции Yellow Sign,
   напечатанные на карте; носитель значения — поле, а не планшет; на нашем поле
   их нет. Без них **Братство Сингулярности не может выиграть в принципе** —
   четыре задачи из шести и блок «Общины» завязаны на символы, потолок фракции
   три технологии из шести при шести необходимых. Выбор: нанести символы на поле
   или переписать четыре задачи Братства и блок «Общины». Решение `D-041`
   по `PLAY-002` этим снимается: его обоснование на проверке не подтвердилось.
2. **`PLAY-030`, третий пункт.** Способности с пометкой «в любой момент»: книга
   такого момента не описывает. Может ли такая способность прервать чужое
   действие в середине — ждёт Alek.
3. **`PLAY-023` и `PLAY-035`** — компонентный бэклог (`D-004`). Раньше прочих
   решать: «Пастух» назван промышленным роботом (от этого зависит число карт
   скрытого влияния) и два чтения «Карательной операции».

### Что поймала проверка батча

Независимый проход по внесённому нашёл шестнадцать мест. Все исправлены
в тот же день; три стоят того, чтобы их запомнить.

**Я в третий раз переопределил `D-037`.** В §7.9 было написано, что вторым
действием годится действие любой категории, «включая действие варианта игры», —
а §11.5 и §11.6 дословно по `D-026` говорят обратное: выполняя действие варианта,
вы не выполняете в этот ход ни общего, ни уникального. Связка «общее действие →
способность → Извлечение сознания вторым» делала ровно запрещённое. Самое обидное:
предложение `PLAY-019` прямо предупреждало «сверить с `D-037`, чтобы
не переопределить пятую категорию во второй раз». Исправлено: вторым действием
годится общее, уникальное или по требованию задачи, действие варианта — нет.

**Правило «текст способности побеждает книгу» вышло сильнее, чем нужно.** Оно
столкнулось сразу с четырьмя безусловными местами: §8.7 (способность,
распоряжающаяся чужим отступлением, подчиняется тем же ограничениям — это `D-038`
F3), §3.4 (предел резерва абсолютен), §11.5 (энграмму нельзя вывести никакой
способностью), §8.3. И в оригинале такого сквозного правила нет вовсе — это наше
обобщение. Сужено: из правила, объявленного безусловным словами «никакая
способность», «никогда», «ни для каких целей», исключений не бывает.

**Правило про юнит-фабрику было шире оригинала.** Оригинал сквозного правила
не даёт и решает по каждому месту отдельно, иногда в разные стороны. Добавлены два
исключения, оба из FAQ: такая фабрика не закрывает собой одновременно требования
«контролируемая фабрика + ваш боевой робот» («No, because Yog-Sothoth is not
technically a Controlled Gate»), и обычная фабрика имеет перед ней приоритет там,
где право отдаётся контролирующему фабрику в конкретном регионе («a normal Gate
in the Overlook takes precedence over Yog-Sothoth»). Заодно снята фраза
«уничтожается обычная» — источника у неё нет, а единственная улика в оригинале
говорит, что уничтожаются обе.

Остальное — незакрытые хвосты той же правки: §7.3.4 остался с «за всю партию»,
§7.3.3 — со старой моделью «на планшете написано, сколько кубиков бросает робот»,
§8.1 — с «выводится только тактическим эффектом», §1.2 и §1.3 — с «пехотинцами»
вместо рекрутов, §6.3 шаг 2 не знал про перечёркнутые деления, §7.1 держал два
разных условия окончания фазы подряд.

**Урок прежний и уже записан в `D-036`:** правка правила не закончена, пока
не пройден сплошной поиск по обоим файлам. Пять из шестнадцати находок — ровно
это. И новое: **предложение в самой записи issue часто называет все места,
которые надо тронуть** — `PLAY-032` называл §7.3.4, `PLAY-037` требовал поиска
по §8.1, `PLAY-021` называл §1.2. Читать предложение до конца дешевле, чем потом
искать.

**Связанные issue:** `PLAY-005`…`PLAY-037`, `PLAY-002`

---

## D-043 — Символы регионов рисуются на поле; правила о них сказали всё, что должны

- **Дата:** 2026-09-16
- **Контекст:** `PLAY-025`. Я вынес вопрос как выбор из двух вариантов — нанести
  символы на поле или переписать четыре задачи Братства.
- **Решение Alek:** «здесь нет никакого выбора и никаких вариантов. Безусловно
  символы будем отрисовывать на поле, никакие другие варианты никогда
  не рассматривались. Просто надо было уточнить явно».
- **Моя ошибка в постановке вопроса.** Спрашивать надо было не «что делаем»,
  а «где они стоят». Второй вариант я придумал сам и подал его как равноправный.
- **Перепроверка по документам и реестрам, без картинок** — по прямому указанию
  Alek. Размещения символов действительно нет нигде:
  - `registry/map/glyphs.yaml` описывает стартовые глифы фракций и ориентиры;
  - `SPEC-BOARD.md` §8 знает **один** знак на полотне — метку Глобал Петролеум
    в Европе, и это про стартовый регион, а не про символы;
  - `regions.yaml` поля под символы не имеет вовсе.

  Сами символы при этом давно готовы: `ICON-032` капля, `ICON-033` пламя,
  `ICON-034` вагонетка, глифы `Q`, `W`, `E` в `Templates/Fonts/Oil Wars.ttf`,
  статус `OK`.
- **Сторона правил закрыта.** В §1.4 добавлен абзац: часть регионов несёт символ,
  символов три, сам по себе символ ничего не делает, значение задаёт карта
  задачи, регионы без символов тоже есть. Больше книге знать нечего — сколько
  регионов несёт каждый символ, решает компонент (`D-021`).
- **Сторона карты открыта:** `MF-011` в `registry/map/findings.yaml`, строка 2
  в `SPEC-BOARD` §11. Там же записано, откуда брать позиции: в оригинале это три
  Spellbook Glyph фракции Yellow Sign, напечатанные на карте Земли (SRC-RB-NEW,
  с. 64: «you may set up in an Area containing one of Yellow Sign's **three
  Spellbook Glyphs**»). При размещении проверить, что на каждой из четырёх
  раскладок есть все три символа и хотя бы один регион без символа, — иначе
  четвёртая задача Братства невыполнима.
- **Урок:** вопрос автору формулируется по тому, что в нём действительно
  не решено. Если решение очевидно и давно принято, вопрос звучит как «где
  это записано», а не как «какой из вариантов выбираем».
- **Связанные issue:** `PLAY-025`, `PLAY-002`, `MF-011`

---

## D-044 — Печать поля: типографское полотно, форматы офисной бумаги не упоминаются

**Дата:** 2026-09-16. **Контекст:** тема всплыла в четвёртый раз — в промпте
на разведку полярной проекции снова оказалась колонка «листов A4».

Alek: «Нигде и никогда не хочу видеть про листы A4 в контексте карты.
Не считаем их, не планируем и не упоминаем. Печатать будем в типографии
на баннере или неопрене, скорее всего. В любом случае не A4 никак.»

### Что это меняет

- Носитель поля — **цельное полотно, печать в типографии** (баннер или
  неопрен). Решение № 2 в `SPEC-BOARD.md` переписано.
- Раскладка полотна на листы **не нормируется, не считается и не упоминается**:
  ни в спецификации, ни в расчётах, ни в оценках вариантов, ни в ответах
  оператору. Это запрет на тему, а не уточнение числа.
- Разрешение исходника остаётся 300 dpi (широкоформатная печать принимает 150,
  но уменьшить всегда можно, вернуть — нет). Это решение № 2 не затрагивает.

### Что вычищено

| Файл | Было |
|---|---|
| `registry/map/SPEC-BOARD.md` | решение № 2 «нарезка под A4», абзац «Тестовая печать на A4», запись v0.2 в журнале |
| `registry/map/EXPERIMENT-POLAR.md` | вводные §0.1 и колонка «Листов A4» в обеих таблицах §1 |
| `registry/issues.yaml` | формулировка в `MISS-023` |
| `DECISIONS.md` | формулировка в разборе `D-041` |
| `CLAUDE.md` | добавлен запрет явным абзацем в блоке «Наше поле» |

Числа в таблицах `EXPERIMENT-POLAR.md` от снятия колонки не изменились:
вердикт по формам был выведен из площади и вписанного круга, а не из числа
листов.

### Открытый хвост

Два файла `Карта/карта_А4_*_print.pdf` (по 170–176 МБ) — наследие домашнего
пробника. Компонентом не являются. Удалять или переименовывать без согласования
нельзя, поэтому пока лежат как есть; спросить у Alek отдельно.

- **Связанные issue:** `MF-012`, `MISS-023`

---

## D-045 — Батч P11: двадцать семь эксплойтов red team

**Дата:** 2026-09-17. **Контекст:** фаза P11 нашла 27 эксплойтов — 18 наших
и 9 помеченных `ORIGINAL PARITY ISSUE`.

### Правило фазы отменено автором

P11 работала по правилу «особенность, которая есть и в оригинале, не чинится».
Alek его снял: **«то, что в оригинале есть дыра, не значит, что её не нужно
закрывать. Это повод для дополнительного исследования и поиска решений —
на форумах, BGG или продумать самим. Мы стремимся к оригиналу, но разумные
отступления, которые улучшат результат и экспириенс, допустимы».**

Все девять parity-находок пересмотрены под этим углом, с внешним поиском.
**Итог: восемь из девяти оказались не дырами, а заявленным дизайном** — и по
пяти нашлись прямые слова автора оригинала или ответ FAQ. Девятая (`RED-021`)
получила оговорку в тексте.

Это важный результат сам по себе: правило «не чиним» давало верный ответ
в восьми случаях из девяти, но **по неверному основанию**. Теперь основание
записано: не «в оригинале так же», а «проверено, и вот чем это держится».

### Итог батча

**16 применено, 9 оставлено без правки, 2 ждут Alek.**

### Пять блокирующих

**`RED-001` и `RED-002` — мои вчерашние правки.** Обе внесены решением `D-042`
и обе оказались неверны:

- §8.4 «боевая сила считается для каждого отряда отдельно» умножала формулу
  на число отрядов: четыре «Саранчи» давали 12 кубиков вместо трёх. Оригинал
  разбирает ровно эту пару числами: «Angela rolls her 3 dice (the amount she
  gets for having 4 remaining Undead in the Battle)». Внесено различение двух
  видов формул плюс пример числами;
- §7.9 «повторить то же самое действие можно» давало неограниченную цепочку
  действий за ход. В оригинале в обоих текстах стоит слово, которое мы
  потеряли: «You may then take a **SECOND, DIFFERENT** Action».

**`RED-003` и `RED-004` — потерянные правила FAQ.** Оба обязаны были войти
в тело книги по `D-003`/`D-022` и не вошли, потому что их нет в `baseline.yaml`.
Это ровно тот случай, который разбирал `D-038`: **отсутствие записи в эталоне
не доказывает, что правила нет.**

- бесплатное действие замораживало фазу действий навсегда; FAQ отвечает по имени
  способности: «Sleeper cannot use Lethargy if no one else is active»;
- способность, дающая действие перед чужой битвой, давала бесконечную цепочку
  битв; FAQ: «you can only do 2 total battles in a row».

**`RED-006` — Луны нет в правилах.** Ждёт Alek, см. ниже.

### Что изменилось в понимании

**`RED-014`: адрес правки был неверен.** Находка указывала на карту, а потеряно
правило тела рулбука — «эффекты фазы влияния срабатывают в вашу очередь»,
то есть один раз за фазу. Чинится §6.5, а не компонент.

**`RED-015`: предложение находки противоречило оригиналу.** «Результат, уже
преобразованный, обратно не преобразуется» — не то правило. Оригинал разрешает
цепочку и разрешает её **порядком**: сначала эффекты, говорящие о выброшенных
результатах, затем остальные. При таком порядке эксплойта нет вовсе.

**`RED-016` — ложная находка**, снята. Текст оригинальной карты найден дословно,
наша карта даже строже оригинала. Попутно закрылось второе чтение `PLAY-035`.

**`RED-018` — переоценена.** Совет про окружение взят из врезки оригинала;
находка приписала ему цель, которой в нём нет. Осталась редакторская оговорка.

### Попутно: из книги ушла склейка краёв карты

`CLAUDE.md` и `D-044` фиксируют: наше поле — южно-полярная проекция, край листа
и есть край карты, склейки нет. §1.4 и статья глоссария «Смежный регион»
до сих пор печатали «регионы у правого края смежны с регионами у левого», §3.3 —
«смежность идёт и через край карты». Снято во всех трёх местах.

### Два вопроса Alek

1. **`RED-006`, Луна — носитель правила.** Правило оригинала потеряно целиком,
   и без него Moon Systems не имеет ни одной контролируемой фабрики за партию:
   всегда 0 нефти за фабрики, всегда 0 влияния, Извлечение сознания недоступно,
   стартовая расстановка §2.4 невыполнима. Луна — не вариант игры и не свойство
   юнита, а **регион**, на который ссылаются §3.3, §3.6, §4.1, §6.2, §7.3.5,
   §8.7. Рекомендация: абзац в §1.4 плюс подраздел в части 3 рядом с §3.6.
   Альтернатива: подраздел части 11 по образцу §11.5.
2. **`RED-012`, невыполнимое требование задачи.** Оригинал даёт оговорку полноты
   только уникальным действиям; у действий по требованию задачи её нет.
   Вариант А (рекомендуемый): требование, направленное на всех противников,
   выполняется для каждого, для кого выполнимо; невыполнимая часть пропускается.
   Вариант Б: нельзя выполнить не целиком — но тогда две задачи Братства
   становятся невыполнимыми при части составов.

### Компонентный след (`D-004`)

Восемь правок компонентов: «Специальная операция» (три ограничения),
«Оперативная пауза» (условие FAQ), «Взлом систем» (один ритуал, не второй),
«Шантаж» (формула переговоров), «Жертва» (область и выбирающий), «Роберт Клоуз»
(пехотинец и отсутствие фабрики), планшет Moon Systems (`FAC-002`, `FAC-010`,
нефть с андроидов), планшет Североамериканского Альянса (формула вместо нуля).

### Что поймала проверка батча

Независимый проход нашёл пятнадцать мест; все исправлены в тот же день. Четыре
стоит запомнить.

**Я снял верную оговорку на ложном основании.** §3.4 «предел резерва безусловен»
я обосновал тем, что в «Балансе сил» берётся фигурка не из вашего резерва.
Для ветки «игрок слишком слабый» это неверно, и оригинал говорит обратное:
«simply add the gray acolyte to his pool. He now has a pool of 7 acolytes from
which to recruit». §3.4 теперь называет два исключения явно.

**Перечень безусловных правил в §3.9 снова не покрыл того, что защищал `D-042`.**
§8.3 («ни один его боевой эффект») и §8.7 (`D-038` F3) маркерных слов не несут
и в список не попали — то есть по букве §3.9 их перебивает текст способности.
Это ровно тот урок, ради которого список и заводился. Оба дописаны, и к маркерам
добавлены «никто» и «ни один».

**«Две битвы подряд — предел» отменяло Свободную битву** — главный приз шести
технологий. Формулировка была шире оригинала: FAQ ограничивает только цепочку
через способность, а Unlimited Battle прямо разрешает «as many Battles as you
wish». Сужено до битв **вне своего хода**, Свободная битва оговорена явно.

**Из трёх ограничений FAQ по внеочередному действию я перенёс два, и потерял
то, которое реально гасит цепочку:** «may not do Unlimited Battle (one regular
Battle is okay)». Плюс первое ограничение было шире оригинала — в FAQ стоит
«**if tied to Map Areas**», и без этой оговорки блокировалось действие
по требованию задачи вида «потратьте 4 нефти». Оба поправлены.

Остальное — незакрытые хвосты тех же правок: «обычной» фабрики не хватало
в §7.3.4, §3.6 и глоссарии; §7.1 снова держал два условия окончания фазы;
§8.2 не знал про новый порядок преобразующих эффектов; §6.5 отсылал к очереди,
которой в фазе ресурсов нет; глоссарные статьи «В игре» и «Резерв» не были
дописаны, хотя предложение находки называло их прямо.

**Урок к `D-036` и `D-042`:** дважды подряд правка не доходила до глоссария,
и дважды предложение в самой записи issue называло нужные статьи. Читать
предложение до конца — дешевле.

### Что в этом батче наше, а не оригинала

Два правила внесены как наше обобщение, и это стоит знать при следующей сверке:

- **порядок преобразующих результат эффектов** (§8.5). В оригинале общего
  правила нет, есть только ответы FAQ по конкретным парам. Наш порядок даёт
  для этих пар тот же исход, но как общее правило он шире источника;
- **правило переговоров** (§3.9). В оригинале это текст карты, а не правило
  рулбука; фраза «игрок, которому нечем платить, в переговорах не участвует»
  основания в источнике не имеет.

**Связанные issue:** `RED-001`…`RED-027`, `PLAY-035`

---

## D-046 — Символы регионов: размещение снято с оригинала и повторяется один в один

- **Дата:** 2026-09-17
- **Контекст:** `MF-011`, последний открытый вопрос по карте. `D-043` закрыл
  «рисовать ли символы» («безусловно будем»), но оставил открытым «где они
  стоят» — размещения не было ни в одном файле реестра.

### Где нашлось

В папке `Карта/` всё это время лежал растр **обеих сторон настоящего поля
Cthulhu Wars**: `карта.jpg` (11024×5523, плашка «EARTH / 3 PLAYER») и
`карта_5.jpg` (10630×5287, «5 PLAYER»). Источником он заведён не был — паспорт
папки в `sources.yaml` описывал только наш PSD и перечислял соседние файлы как
«растровые версии того же». Для `карта 5.jpg` (с пробелом) это верно — это наша
схема с русскими подписями. Для `карта.jpg` и `карта_5.jpg` — нет. Файлы
различаются пробелом и подчёркиванием в имени. Заведено как `MF-013`,
источник — `SRC-CW-MAP-SCAN`, приоритет 1: это сам компонент, а не разбор
компонента.

Имена трёх глифов взяты с вики Yellow Sign (`SRC-WIKI-YS`): **Dragon**,
**Thorns**, **Chevron**. Каждому соответствует одно требование заклинания
«Successfully Desecrate an Area marked with this Glyph». Имя Chevron
независимо подтверждает рулбук (`SRC-RB-OLD`, правила карты Yuggoth).

### Что на карте

**Закон размещения: символ идёт по материку.**

| Наш знак | Оригинал | Материки | «3» | «5» |
|---|---|---|---|---|
| капля `Q` (`ICON-032`) | Dragon Glyph | обе Америки | 2 | 5 |
| пламя `W` (`ICON-033`) | Thorns Glyph | Европа и Азия | 2 | 5 |
| вагонетка `E` (`ICON-034`) | Chevron Glyph | Африка и Австралия | 2 | 4 |

Каждая область суши, **кроме Антарктиды**, несёт ровно один символ: шесть
из шести на стороне «3», четырнадцать из четырнадцати на стороне «5». Шесть
океанов и Антарктида — ни одного. Дети области стороны «5» наследуют символ
родителя без исключений.

Форма знаков в оригинале: Dragon — свёрнутый в кольцо завиток, Thorns — пучок
из пяти шипов, Chevron — «^». Thorns легко принять за язычки пламени; в первом
проходе так и вышло, и это же совпадение оказалось удобным.

### Решение Alek

«Расположение строго как в оригинале. Соответствие лучше сделать
тематически-реалистично: капля — нефть, вагонетка — уголь/руды, пламя —
природный газ. Соответственно, в идеале выбрать соответствие по регионам, где
в реальности преимущественно соответствующие ресурсы сейчас добываются. НО!
Тематически только соответствие берём и насколько возможно, само расположение
символов должно ПОЛНОСТЬЮ соответствовать оригиналу, меняются только значки.»
И отдельно: «Сначала перепроверить, убедиться что суши без значков нет.
Ничего не сокращать, не перемещать, соответствие оригиналу на 100 % для всех
вариантов карты на 3, 4 и 5 игроков.»

Тематическое соответствие легло без натяжки: нефть на Америки (сланец, нефтяные
пески, Венесуэла, Бразилия), газ на Скандинавию, Европу, Азию и Аравию (Норвегия,
Россия, Катар, Иран), уголь и руды на Африку с Австралией — главный горнорудный
континент. Побочно сошлось и с игрой: Глобал Петролеум стартует в Европе, то есть
сразу в регионе `W`, — ровно как Yellow Sign в оригинале, которому рулбук
советует осквернять Европу первой.

### Перепроверка

Сплошной просмотр обеих сторон плитками по шесть штук, плюс отдельные кропы
на местах, где знак стоит близко к границе: Запад и Восток Северной Америки,
Скандинавия и Европа, Австралия и Новая Зеландия. Автоматический поиск по цвету
и сопоставление с шаблоном пробовались и **не сработали**: цветовой баланс двух
сканов разный, размер знака гуляет (европейский шип мельче азиатского), а
декоративная графика материков попадает в ту же цветовую маску. Перечень снят
глазами; машина проверяет уже записанное.

`tools/map.py check` теперь требует: ровно три символа, ни одной области с двумя
символами, каждая суша кроме Антарктиды с одним, океаны и Антарктида без,
наследование по `parent`. `build` кладёт символ в каждую вершину и считает
сводку. Проверено по всем четырём раскладкам:

| Раскладка | `Q` | `W` | `E` | без символа |
|---|---|---|---|---|
| `MC-3P` | 2 | 2 | 2 | 7 |
| `MC-4P-A` (3-5) | 2 | 5 | 3 | 7 |
| `MC-4P-B` (5-3) | 5 | 2 | 3 | 7 |
| `MC-5P` | 5 | 5 | 4 | 7 |

Условие `D-043` («все три символа и хотя бы один регион без символа в каждой
раскладке») выполнено везде.

### Что это меняет в документах

`ТЗ-ЭСКИЗ-КАРТЫ` §7 говорил «зарезервировать три области под символы
и убедиться, что хотя бы одна область остаётся без символа». **Это оказалось
неверно в обе стороны:** резервировать надо не три области, а все области суши
кроме Антарктиды — шесть на стороне «3» и четырнадцать на «5»; а свободных
областей не одна, а семь, и все они — вода и Антарктида. Отсюда следствие,
которое стоит помнить при балансе: **четвёртая задача Братства Сингулярности
(«община в регионе без символов») выполняется только в океане или
в Антарктиде.** Это не наша особенность — в оригинале четвёртый собор
The Ancients ставится туда же и по той же причине.

| Файл | Что внесено |
|---|---|
| `registry/map/glyphs.yaml` | раздел `region_symbols` (`MS-001`…`MS-003`), пояснение в `meta`, предупреждение в шапке про два разных знака |
| `registry/map/SCHEMA.md` | описание раздела, ID-пространство `MS-###`, перечень проверок |
| `registry/map/SPEC-BOARD.md` | новый §8.2 «Символы регионов»; старые §8.2 и §8.3 сдвинуты в §8.3 и §8.4; решение № 15; §11 закрыт; журнал v0.8 |
| `registry/map/ТЗ-ЭСКИЗ-КАРТЫ.md` | §7 переписан: таблица символов и правильное требование к эскизу |
| `registry/map/findings.yaml` | `MF-011` CLOSED, заведена `MF-013` |
| `registry/icons.yaml` | `original_concept` у `ICON-032/033/034`, разбор соответствия в заметке `ICON-034` |
| `registry/sources.yaml` | `SRC-CW-MAP-SCAN`, `SRC-WIKI-YS`; исправлена заметка `SRC-WIKI-MAP` |
| `tools/map.py` | `symbol_index`, `check_region_symbols`, символ в каждой вершине сборки, раздел отчёта |
| `out/11-map.md` | раздел «Символы регионов» с пофамильной разбивкой по всем раскладкам |

### Открытый хвост

`MF-004` — три глифа фракций и две подписи-ориентира со стороны «3» так
и стоят с доверием MEDIUM, потому что снимались с фотографий. Теперь есть
растр лучшего качества, по которому их можно довести до HIGH. В эту работу
не входило; `D-043` и `MF-004` уже решили, что специально за этим не гоняемся,
но повод появился.

- **Связанные issue:** `MF-011`, `MF-013`, `MF-004`, `PLAY-025`

---

## D-049 — Луна уходит на планшет; невыполнимое требование решено FAQ оригинала

**Дата:** 2026-09-17. Два вопроса, оставшиеся открытыми после `D-045`.

### `RED-006` — все фракционные особенности на планшете

**Решение Alek:** «У нас все фракционные особенности печатаются на планшете
фракции и её компонентах. Это лучше, чем разделы в правилах, которые нужно
искать — планшет и компоненты всегда перед глазами».

Отдельного раздела про Луну в книге не будет. В правилах осталась только
зацепка, без которой такой планшет нелегален, — три строки в §3.3: отдельные
фракции приносят на стол собственный регион, он лежит рядом с планшетом и для
всех правил считается таким же регионом поля, а его особенности описаны
на планшете. Никакой фракционной конкретики (`D-021`).

Остальное уже работает: §3.6 после `D-042` знает фабрику, которую нельзя взять
под контроль, — «так бывает устроен отряд или **тайл**; где это так, сказано
на планшете фракции», — и она всегда считается контролируемой владельцем. Луна
подходит под это определение целиком.

**Компонентная часть — блокер печати планшета Moon Systems** (`D-004`). На него
обязано попасть, дословно по оригиналу (SRC-RB-NEW, с. 75): шесть андроидов
на Луне при подготовке; Луна считается сухопутным регионом с фабрикой
Moon Systems под контролем для любых целей, и контроль не отбирается; другую
фабрику туда не построить и не переместить; отряды Moon Systems свободно
перемещаются и отступают между Луной и любым регионом поля; отряды других фракций
могут уйти с Луны, но попасть на неё перемещением или отступлением не могут.
Плюс `FAC-002`, `FAC-010` и отдельная находка: в оригинале все кошки дают
по 1 Power в фазу ресурсов, а наши андроиды нефти не дают.

### `RED-012` — решение автора не потребовалось, ответ нашёлся в FAQ

Alek спросил, точно ли это нигде не разобрано. Разобрано — во встроенном FAQ
рулбука, и прямым общим принципом:

> **Q.** How do the Ancients generally interact with requirements or rules
> necessitating all Factions to have a Great Old One?
> **A.** **The Ancients are ignored for this purpose.** For example, the Ancients
> need not have a Great Old One for an independent Azathoth's controller to earn
> his Nuclear Chaos Spellbook.

То есть фракция, которая физически не может удовлетворить требование,
**игнорируется** для него, а требование продолжает работать для остальных.
Это и есть вариант А, и он теперь не наше решение, а перенос.

Соседний ответ показывает **границу** принципа: «Crawling Chaos can never get
his Capture Spellbook requirement from Bubastis, which means you can't use
Crawling Chaos and Bubastis as opponents in a 2-player game». Когда требование
завязано на одного конкретного противника, оригинал правила не выдумывает,
а предупреждает о составе. Наш случай другой — требование адресовано каждому.

Третий ответ подтверждает и саму пару карт: «Can Opener of the Way use They Break
Through when the Ancients **allow everyone to Summon** their cheapest / most
expensive monster? — Yes» — это разрешение, а не обязанность.

Внесено в §7.5: «Требование, направленное на всех противников, выполняется для
каждого, для кого оно выполнимо. Противник, для которого выполнить его физически
нельзя — нечем, некуда или незачем, — просто пропускается, и требование считается
выполненным. Если оно невыполнимо ни для кого, выполнять его нельзя».

**Урок:** вопрос «уверен, что нигде не разобрано?» стоил одного поиска по FAQ
и снял решение автора. Прежде чем выносить механику на выбор — дочитывать FAQ
до конца, включая разделы дополнительных фракций.

**Связанные issue:** `RED-006`, `RED-012`

---

## D-047 — Поле описывается без носителя; компонентная часть 6–8 из книги уходит; полковники — вариант, включённый по умолчанию

**Дата:** 2026-09-22. **Контекст:** приёмка P12. Alek прошёл чистовик и снял
четыре места, где книга описывает не правило, а то, как компонент физически
устроен. Общий принцип решения: **книга говорит, что делать, а не из чего это
склеено.** Форма компонентов ещё не закреплена, и всё, что от неё зависит,
из текста уходит, пока не станет фактом.

### 1. Игровое поле: только универсальные формулировки

Alek: «Скорее всего у нас будут готовые поля односторонние не из половинок,
но это не 100 % решение. В правилах про игровое поле нужно оставить без лишних
деталей, только универсальные общие формулировки: возьмите игровое поле на ваш
состав и разложите в центре стола».

Из книги уходят **две части, стороны «3» и «5», половины и сборка**:

| Место | Было | Стало |
|---|---|---|
| §1.1 | «Игровое поле, карта Земли — 2 части, каждая двусторонняя. **В партии всегда собираются две части** (2.1)» | «Игровое поле, карта Земли» |
| §1.4 | «Поле состоит из двух частей, и каждая часть напечатана с обеих сторон. Очертания материков на обеих сторонах одинаковы, но сторона на 5 игроков разбита на большее число регионов…» | «Поле выпускается под разные составы игроков. Чем больше игроков, тем на большее число регионов разделены те же материки» |
| §2.1 | заголовок «Соберите поле», таблица с колонкой «Как класть» | заголовок «Разложите игровое поле», таблица «Игроков — Регионов» |
| §2.1 | «Стороной „5“ может лежать любая из двух половин… кладите стороной „5“ ту половину, где Европа, Азия и Африка» | «На четверых поле бывает двух вариантов, и это разные партии… Играйте на любом» |
| §2.4 | «Сторона „5“ дробит материки на большее число регионов, чем сторона „3“… „Западная Африка“ есть только на стороне „5“» | «Планшет называет самый узкий из регионов, а поля на меньший состав дробят материки не так мелко… „Западная Африка“ есть не на каждом поле» |
| глоссарий, «Стартовый регион» | «если названного региона в собранной раскладке нет» | «если названного региона на вашем поле нет» |

**Что сохранено намеренно:**

- **числа регионов 13 / 17 / 21 остаются** — это `D-024` пункт 3, они печатаются
  как проверка того, что перед игроками нужное поле. Отменять решение никто
  не просил, а проверка работает и для готового одностороннего поля;
- **две раскладки на четверых остаются** (`PLAY-029`, `D-042`): это разные
  партии, и книга обязана сказать, что выбор есть. Ушло только умолчание
  «кладите стороной „5“ ту половину, где Европа», — оно сформулировано
  через половины и без них не существует. **Вернуть, когда форма компонента
  определится**, если умолчание вообще понадобится;
- §2.2 «возьмите тот трек и ту его сторону, которые соответствуют числу
  игроков» — это правило (какие деления работают), а не описание носителя.
  Остаётся.

### 2. Компонентная часть состава 6–8 из книги убирается

Alek: «Всё, что касается 6–8 игроков в плане компонентов, просто убери, оставь
только изменения в правилах, если они есть».

**Проверено: отдельных правил для 6–8 в книге нет.** Всё, что там про 6–8
стояло, описывало компоненты: три строки таблицы раскладок с прочерками и абзац
«поле на 6–8 в комплект не входит». Единственное место, где число игроков меняет
игру, — шкала трека Судного дня, и §2.2 формулирует это универсально, без
упоминания 6–8.

Убрано:

- §1.4 — абзац «Партию можно сыграть и на 6–8… но поле и прочие компоненты под
  него в комплект не входят». Осталась одна строка: «Игра рассчитана
  на 3–5 участников»;
- §2.1 — три строки таблицы для 6, 7 и 8 игроков и абзац под ней
  «Поле на 6–8 игроков в комплект не входит».

**Это отменяет два пункта `D-040`:** пункт 2 в части «та же оговорка под таблицей
раскладок в §2.1» и пункт 7 «прочерки в таблице регионов для 6/7/8 остаются».
Пункт 2 в части «диапазон снят с титула» остаётся в силе. Пункты 1 и 3–6, 8, 9
`D-040` не затронуты.

### 3. Полковники — вариант, включённый по умолчанию

Alek: «Они у нас сразу пропечатаны на планшете и включены по дефолту. Нужно
переформулировать в виде: это необязательное правило, по умолчанию они включены
у вас на планшете; если не хотите играть с этим вариантом, просто игнорируйте
описание полковников на планшете и отложите в сторону фигурку».

§11.2 открывается абзацем: вариант включён по умолчанию, отдельно ничего
добавлять не нужно, отказ — отложить фигурку и не читать строку планшета.
Остальной текст §11.2 не меняется.

**Почему это не ломает часть 11.** Преамбула части 11 говорит: «если этих
компонентов на столе нет, раздел просто не читайте». Для полковников компонент
на столе есть всегда, потому что он напечатан на планшете, — и без оговорки
читатель обязан был бы считать вариант включённым молча. Оговорка снимает
ровно это.

**`D-021` не затронуто:** свойство полковника («Боевое донесение») по-прежнему
живёт на планшете, книга его не расписывает.

### 4. «Очередь»: цифр в книге нет и не было

Alek: «Очередь разбираю прямо сейчас, зафиксируем в реестре фракций. В правилах
цифры не нужны, только упоминание „смотрите на планшете“».

**Проверено сплошным поиском: в книге нет ни одного числа „Очереди“.** §2.4
говорит ровно то, что просил Alek: порядок задан числом «Очередь» в блоке
«Подготовка» на планшете, чем меньше число — тем раньше фракция расставляется.
Это `D-031` пункт 1 и `D-034` пункт 4, оба уже исполнены. Правка не требуется.

### 5. Четыре находки переклассифицированы: книгу они не блокируют

В QA-таблице P12 они стояли в списке «что блокирует печать». Разбор Alek
показал, что три из четырёх не блокируют ничего, а четвёртая — обычная
компонентная работа.

- **`MISS-014`, боевая сила Роя.** Вопрос Alek: «А разве у нас уже свёрстана
  карта?» — **нет, карта Контроля сети в проекте не сделана**, и речь была
  про неё, а не про оригинал. Число **2** обязано появиться на ней при вёрстке
  (`D-040` пункт 4). Текст книги правильный и правки не требует: характеристики
  отрядов живут на компонентах (`D-021`).
- **`DEV-014`, стоимость карт лояльности наёмников.** Вопрос Alek: «Не понял,
  в чём проблема». **Проблемы в книге нет** — запись заведена по черновику,
  где цена стояла дважды и по-разному. В книге числа не печатаются ни разу:
  §6.6, §11.4.1 и §11.4.2 говорят «оплатите стоимость, указанную на карте».
  Остаётся одна компонентная заметка, уже записанная в `D-035`: **цена карты
  лояльности включает и влияние, и нефть — 2 и 2**, это надо проставить
  при вёрстке карт.
- **`MISS-025`, «Стелс».** Alek: «Нужно разобрать, но должно быть указано
  на карте/планшете». Согласовано: эталон — карта, приложение по фракциям
  архивное (`D-028`). На §8.3 это не влияет, он написан по общему правилу
  оригинала.
- **Символы регионов.** Alek: «Поля ещё нет, только описание в реестре, там
  должно быть зафиксировано, какой символ в каком регионе». **Так и есть:**
  `registry/map/glyphs.yaml`, раздел `region_symbols` (`MS-001`…`MS-003`),
  закон размещения «символ идёт по материку», и `map.py check` требует, чтобы
  каждая суша кроме Антарктиды несла ровно один символ, а океаны и Антарктида —
  ни одного. Проверено по всем четырём раскладкам. `MF-011` закрыт `D-046`;
  нанесение на полотно — работа по карте, не по книге.

**Следствие для отчётов P12:** список «что блокирует печать» в `14-final-qa.md`
и `15-release-notes.md` сокращается до того, что действительно блокирует, —
числа «Очередь» (Alek разбирает сейчас) и правила Луны на планшете Moon Systems.
Остальное — компонентный бэклог `D-004`.

### 6. Порядок внесения

Правки внесены **в черновики**, `RULEBOOK.md` пересобран скриптом. Иначе
черновики и чистовик разошлись бы, и следующая сборка молча откатила бы правку.
После каждой правки — сплошной поиск по обоим файлам (`D-036`), глоссарий
включительно.

### 7. Что поймала проверка батча

Независимый проход по внесённому — субагенту не сообщалось, кто правил, — нашёл
шесть мест. Все исправлены в тот же день; четыре стоят того, чтобы запомнить.

**Правка сняла признак, по которому раскладки различались.** §2.1 остался
с «на четверых поле бывает двух вариантов», но чем они различаются, больше
не говорил: старое различие было сформулировано через половины и стороны, и ушло
вместе с ними. Пересчёт регионов на четверых не помогает — в обоих вариантах 17.
Дописано различие, взятое из реестра карты, а не придуманное: в одном варианте
дробно нарезаны Европа, Азия и Африка, в другом — обе Америки и Океания.
**Умолчание `D-042` при этом восстановлено** в новых словах: «если не хотите
выбирать — берите тот, где мельче нарезаны Европа, Азия и Африка». Это тот же
`MC-4P-A`, что и раньше.

**Полковники по умолчанию сломали преамбулу части 11.** Преамбула объявляет все
режимы части выключенными, пока их не включили, и учит: «нет этих компонентов
на столе — раздел не читайте». Для 11.2 это ровно наоборот. Дописано исключение
в преамбулу, а строка «Что нужно» в 11.2 переписана: ничего добавлять не надо,
режим включён, при желании выключается.

**Полковника не было в составе фракции.** §1.2 перечислял фигурки — 6 рекрутов,
8–12 боевых машин, 1 боевой робот — и полковника среди них нет, хотя 11.2 теперь
утверждает, что он на столе с самого начала. Дописан.

**§11.3 требовал «вариант с полковниками» как то, что надо доставить.**
Переписано: нужны карты лояльности, а сами полковники уже в игре, если их
не убрали.

Пятое и шестое — редакторские: §1.4 «поле выпускается» читалось как про издания
на рынке, заменено на «поле своё на каждый состав игроков»; в §11.2 добавлено,
что отказ от полковников выключает и уникальных полковников.

**Чего проверка потребовала, а мы намеренно не сделали:** назвать, сколько полей
входит в комплект. Это ровно то, что решение снимает: форма компонента
не закреплена, и до неё книга о ней молчит (`MISS-023`).

**Урок прежний, `D-036`:** правка, снявшая описание компонента, оставила без
опоры правило, которое на это описание ссылалось. Сплошного поиска по словам
правки мало — надо проверять и то, что правкой стало пустым.

- **Связанные issue:** `PLAY-027`, `PLAY-029`, `MISS-023`, `MISS-014`,
  `DEV-014`, `MISS-025`, `MF-011`

## D-048 — «Очередь»: шкала на 12 позиций, три общие «10» разведены

- **Дата:** 2026-09-22
- **Контекст:** поле «Очередь» напечатано на всех восьми планшетах и заведено
  в реестре (`registry/factions.yaml`, раздел `redesign`, ключ `queue`).
  Сверка 2026-09-22 показала: реестр совпадает с напечатанными PDF один в один,
  расхождений нет. Но три фракции делят одно число — **10** у Братства
  Сингулярности, Moon Systems и Сайнтифик Солюшн (`FAC-008`), а JSON планшета
  Сайнтифик Солюшн хранит «8», которая уже занята Североамериканским Альянсом
  (`SRC-003`). Позиции 4, 5, 6 и 9 не заняты никем.
  `D-031` оставил раздачу номеров на потом и назвал блокер: нужна карта
  соответствия наших фракций оригинальным. **Блокер снят** — в реестре восемь
  пар `mapping`, все `CONFIRMED` с `confidence: HIGH`.
- **Решение Alek:** брать полную шкалу на 12 позиций — весь набор оригинала.
  Наши фракции занимают свои места, свободные держатся за фракции оригинала,
  у которых нашего аналога пока нет. Главное требование — совпадение
  с оригиналом.

| Очередь | Фракция | Оригинал | Откуда число |
|---------|---------|----------|--------------|
| 1 | Островная Империя | Great Cthulhu | напечатано |
| 2 | Эйркрафт Корпорейшн | Crawling Chaos | напечатано |
| 3 | Глобал Петролеум | Yellow Sign | напечатано |
| 4 | — резерв | Daemon Sultan | нашего аналога нет |
| 5 | — резерв | The Invasion | нашего аналога нет |
| 6 | **Moon Systems** | Bubastis | было 10 |
| 7 | Клонэйд Ресёрч | Black Goat | напечатано |
| 8 | Североамериканский Альянс | Sleeper | напечатано |
| 9 | **Братство Сингулярности** | The Ancients | было 10 |
| 10 | — резерв | Tcho-Tcho | позиция задана оригиналом |
| 11 | — резерв: Криогеник Технолоджис | Windwalker | гипотеза, confidence MEDIUM |
| 12 | **Сайнтифик Солюшн** | Opener of the Way | было 10 |

- **Что здесь из оригинала.** Три ограничения `SRC-RB-NEW`, все выполнены:
  - Ancients расставляются раньше Tcho-Tcho, Windwalker и Opener (стр. 68) —
    Братство 9 раньше позиций 10, 11 и 12;
  - Tcho-Tcho расставляются после всех, кроме Windwalker и Opener (стр. 89) —
    позиция 10 после всех занятых, кроме 11 и 12;
  - Opener расставляется последним (стр. 81) — Сайнтифик Солюшн 12.
- **Что здесь наше и оригиналу не противоречит.** Конкретные ранги внутри 1…9:
  полного линейного порядка в оригинале нет, нумерации фракций нет вообще
  (проверено сплошным поиском по корпусу, см. `D-031`). Позиции 1, 2, 3, 7, 8
  не трогались — они уже напечатаны. Moon Systems встал на 6 потому, что это
  свободная середина, а не потому, что так сказано в оригинале: у Bubastis нет
  ни одного ограничения по порядку, кроме общего «раньше Tcho-Tcho».
  Moon Systems при этом оказался **раньше** Братства, хотя предложение в `FAC-008`
  звучало наоборот; оригинал этого не запрещает.
- **Чего решение не делает.** Не трогает первенство: «Очередь» задаёт только
  порядок расстановки, `D-031` пункт 1 в силе. Не сжимает шкалу до 1…8 — это
  сломало бы пять уже напечатанных планшетов. Не назначает очередь фракциям
  «Свет Истины» и «Армия освобождения»: у них нет ни материалов, ни
  сопоставления с оригиналом, у обеих `queue: null` и причина в `queue_note`.
- **Следствия:**
  1. Исправлено в `registry/factions.yaml` (`queue` + `queue_note`, шкала целиком
     в `meta.queue_scale`) и в `registry/map/glyphs.yaml`.
  2. Исправлены JSON планшетов: `Братство Сингулярности.json` 10 → 9,
     `Moon Systems.json` 10 → 6, `Сайнтифик Солюшн.json` 8 → 12.
  3. **Три PDF планшета устарели** и требуют перевыкладки из `Template.html`:
     Братство Сингулярности, Moon Systems, Сайнтифик Солюшн. Пока они не
     перевыложены, напечатанный компонент расходится с реестром.
  4. `FAC-008` закрыт, `SRC-003` закрыт.
  5. Текст правил не меняется: §2.4 ссылается на поле планшета, а не на числа.
- **Уточнение того же дня — тип значения.** «Очередь» в реестре хранится
  **дробным числом (float)**, а не целым; целые значения пишутся с хвостом `.0`.
  Причина — гибкость на будущее: если фракция добавится между двумя уже
  напечатанными, ей даётся дробная позиция, и ни один напечатанный планшет
  не сдвигается. Например `1.5` встаёт между Островной Империей (1.0)
  и Эйркрафт Корпорейшн (2.0). Порядок задаётся сравнением чисел, шаг между
  соседями смысла не несёт — 4.0 и 5.0 «зарезервированы» условно, между ними
  влезет сколько угодно.
  Затронуто: `registry/factions.yaml` (`queue`, `meta.queue_scale.slots.n`),
  `registry/map/glyphs.yaml`, обе `SCHEMA.md`, `tools/map.py` (печать через
  `:g`, чтобы в отчёте стояло «1», а не «1.0»).
  **Печатный компонент не меняется:** на планшете стоит «Очередь: 1», хвост
  `.0` не печатается, и в JSON планшетов значение остаётся целой строкой.
  Дробное число, если появится, печатается как есть — «Очередь: 1.5».
- **Связанные issue:** `FAC-008`, `SRC-003`, `MAP-04`, `MAP-06`, `MAP-07`,
  решения `D-031`, `D-020`

---

## D-050 — Глоссарий: сорок девять статей вместо сорока семи, состав заморожен заново

- **Дата:** 2026-09-22
- **Контекст:** `TISS-012`, единственный `FAIL` финальной QA-проверки P12.
  Состав части 13 сверен со `STRUCTURE.md` §13 и с `terms.yaml`: в книге
  49 статей против 47 замороженных, и две из них стоят вне карты терминов.

### Что это было

**Не дефект текста.** Обе статьи написаны верно и нужны книге:

- **«Карта лояльности»** употребляется в правилах семнадцать раз, и первое
  вхождение стоит за 350 строк до объяснения;
- **«Основное действие»** — несущий термин: через него определено свободное
  действие и шаг 2 хода.

Расхождение **процедурное**. Состав глоссария заморожен в P7, а обе статьи
появились позже, в P8–P9: первая — с разбором наёмников, вторая — следствие
`D-037`, который завёл пятую категорию действий уже после фриза. Запись в карте
терминов у первой была (`TERM-020`, `CANONICAL`), у второй не было вовсе.

### Решение

Состав расширен до **сорока девяти** статей, обе остаются.

- Заведена запись **`TERM-080` «Основное действие»** — наш термин, прямого
  соответствия в оригинале нет: там обходятся описанием «you may take only one
  Action per turn» и отдельного имени не вводят.
- В таблицу `STRUCTURE.md` §13 добавлены обе строки, счётчик «сорок семь»
  исправлен, под таблицей записано, что и почему добавлено.
- Состав заморожен заново: следующая правка — только решением.

**Альтернатива — удалить две статьи ради формального соответствия фризу —
отвергнута.** Фриз заведён против молчаливого расползания состава, а не против
обоснованных добавок; книга от удаления стала бы хуже.

Проверка `terminology` в финальной QA-таблице переходит в `PASS`:
**13 из 13.**

### Побочно: коллизия номеров решений

При записи обнаружилось, что номера `D-046` и `D-047` заняты дважды —
параллельные чаты (карта и P12) выдали свои решения в тот же день. Мои
переномерованы: «Луна и невыполнимое требование» — теперь **`D-049`**, это
решение — **`D-050`**. Ссылки в `issues.yaml`, `terms.yaml` и `STRUCTURE.md`
поправлены.

**Заодно исправлена ошибка данных:** `SRC-003` стоял в статусе `DECIDED`
с проставленной датой закрытия — валидатор ругался. По тексту `D-048` вопрос
закрыт, статус приведён к `CLOSED`.

**Правило на будущее:** номер решения берётся **после** перечитывания хвоста
`DECISIONS.md`, а не по памяти о последнем виденном. При параллельных чатах
память врёт.

**Связанные issue:** `TISS-012`, `SRC-003`

---

## D-051 — Четыре последние записи закрыты; компонентный бэклог больше не считается работой по правилам

- **Дата:** 2026-09-22
- **Контекст:** после закрытия P12 в реестре оставались четыре записи со статусом
  `OPEN`, и я трижды подряд выносил их в отчёт как «осталось». Alek: «кажется,
  мы пошли по кругу».

### Почему это повторялось

Ошибка была не в записях, а в том, как я их подавал. Три из четырёх давно
решены — `D-004` (компоненты вне задачи) и `D-021` (числа живут на планшетах
и картах) закрывают их целиком. Статус `OPEN` означал не «нужно решение»,
а «работа лежит в другой задаче», и я раз за разом читал его как первое.

### Что закрыто

**`DEV-014`, цена карт лояльности наёмников, и `MISS-014`, боевая сила Роя.**
Alek: числа будут на самих картах при вёрстке, в правилах не требуются. Книга
уже написана под это и дыры не оставляет: §11.4 велит «оплатите стоимость,
указанную на карте лояльности», §11.6 — «его боевая сила указана на карте
Контроля сети». Число оригинала для Роя — **2** — сохранено в записи
`MISS-014`, его надо перенести на карту при вёрстке.

**`DEV-021`, имя действия призыва меха.** Вопрос был снят ещё `D-032`,
а `OPEN` остался по недосмотру. Проверено по тексту: раздел называется
«7.3.2. Производство боевой машины или меха», в сводной таблице действий он же,
лимит — «за это действие производится ровно один отряд — боевая машина или мех».
**Второго имени не требуется**, общих действий по-прежнему семь. Обоснование
`D-032` держится на эррате, которая слила это сама: «Terrors are summoned
**as though they are Monsters**» — без имени действия.

**`MISS-025`, разнобой формулировок «Стелса».** Разбирается при работе
над фракциями и их картами. Правила от выбора редакции не зависят: §8.3 написан
по общему правилу оригинала и одинаково верен при любой. Эталон при сведении —
карта, она совпадает с оригиналом дословно; приложение по фракциям — черновик.

**Открытых записей в реестре ноль.**

### Правило подачи на будущее

Записи типа `OUT-OF-SCOPE` и всё, что закрыто `D-004`, **не выносятся в отчёт
о состоянии правил как незакрытая работа.** Их место — в задаче по фракциям
и компонентам, и упоминать их в статусе рулбука надо один раз, при передаче,
а не в каждом отчёте.

**Связанные issue:** `DEV-014`, `MISS-014`, `DEV-021`, `MISS-025`
